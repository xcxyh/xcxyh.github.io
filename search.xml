<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>hexo 踩坑</title>
    <url>/2020/05/18/hexo-%E8%B8%A9%E5%9D%91/</url>
    <content><![CDATA[<h3 id="hexo-generate-报错"><a href="#hexo-generate-报错" class="headerlink" title="hexo  generate 报错"></a>hexo  generate 报错</h3><p>ERROR Render HTML failed: 2020/05/18/hello-world/index.html<br>TypeError: Cannot read property ‘replace’ of null</p>
<p>重装 主题 next 解决。</p>
]]></content>
  </entry>
  <entry>
    <title>my new post</title>
    <url>/2020/05/18/my-new-post/</url>
    <content><![CDATA[<h3 id="第一篇-记录一下"><a href="#第一篇-记录一下" class="headerlink" title="第一篇  记录一下"></a>第一篇  记录一下</h3><p>hexo 简单命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new <span class="string">"postName"</span>   <span class="comment">#新建文章</span></span><br><span class="line">hexo new page <span class="string">"pageName"</span> <span class="comment">#新建页面</span></span><br><span class="line">hexo generate <span class="comment">#生成静态页面至public目录</span></span><br><span class="line">hexo server <span class="comment">#开启预览访问端口（默认端口4000，'ctrl + c'关闭server）</span></span><br><span class="line">hexo deploy <span class="comment">#将.deploy目录部署到GitHub</span></span><br><span class="line">hexo <span class="built_in">help</span>  <span class="comment"># 查看帮助</span></span><br><span class="line">hexo version  <span class="comment">#查看Hexo的版本</span></span><br><span class="line">hexo deploy -g  <span class="comment">#生成加部署</span></span><br><span class="line">hexo server -g  <span class="comment">#生成加预览</span></span><br><span class="line"><span class="comment">#命令的简写</span></span><br><span class="line">hexo n == hexo new</span><br><span class="line">hexo g == hexo generate</span><br><span class="line">hexo s == hexo server</span><br><span class="line">hexo d == hexo deploy</span><br></pre></td></tr></table></figure>

]]></content>
  </entry>
  <entry>
    <title>字符串中的动态规划</title>
    <url>/2020/05/21/%E5%AD%97%E7%AC%A6%E4%B8%B2%E4%B8%AD%E7%9A%84%E5%8A%A8%E6%80%81%E8%A7%84%E5%88%92/</url>
    <content><![CDATA[<h3 id="字符串中的DP"><a href="#字符串中的DP" class="headerlink" title="字符串中的DP"></a>字符串中的DP</h3><ol>
<li><p>最长回文子串 longestSubPalindome  单串</p>
</li>
<li><p>最长回文子序列 longestPalindromeSubseq 单串</p>
</li>
<li><p>最长公共子序列  longestCommonSubsequence  双串</p>
</li>
<li><p>编辑距离  minDistance 双串</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">来源：力扣（LeetCode）</span><br><span class="line">链接：https://leetcode-cn.com</span><br><span class="line">著作权归领扣网络所有。商业转载请联系官方授权，非商业转载请注明出处。</span><br></pre></td></tr></table></figure>

</li>
</ol>
<a id="more"></a>

<h4 id="最长回文子串-longestSubPalindome-Leet-5"><a href="#最长回文子串-longestSubPalindome-Leet-5" class="headerlink" title="最长回文子串 longestSubPalindome   Leet  5"></a>最长回文子串 longestSubPalindome   Leet  5</h4><p>题目：给定一个字符串 s，找到 s 中最长的回文子串。你可以假设 s 的最大长度为 1000。</p>
<p>示例 1：输入: “babad”<br>              输出: “bab”<br>              注意: “aba” 也是一个有效答案。</p>
<p>首先考虑最直观的方法，双重for循环枚举出所有的子串，找出哪些是回文子串，并返回最长的那个。显然这个答案会超时。然后考虑一种从中心向两端扩展的方法，具体是以字符串中每一个字符或 每两个一组 为中心，向两端扩展，判断中心左边和右边的字符是否相同，相同就继续扩展，直到得到以当前字符或字符串为中心的最长回文子串，然后 取所有情况中最长的那个。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">int</span> maxLen = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">private</span> String subString;</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">longestPalindrome</span><span class="params">(String s)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (s.length() &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">       <span class="keyword">return</span> s;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; s.length(); i++) &#123;</span><br><span class="line">        extendSubstring(s, i, i);  <span class="comment">//从一位的子串开始扩展</span></span><br><span class="line">        extendSubstring(s, i, i + <span class="number">1</span>); <span class="comment">//从两位的子串开始扩展</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> subString;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 从中心向两端扩展，并更新结果 区间是 [start, end]</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">extendSubstring</span><span class="params">(String s, <span class="keyword">int</span> start, <span class="keyword">int</span> end)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (start &gt;= <span class="number">0</span> &amp;&amp; end &lt; s.length() &amp;&amp; s.charAt(start) == s.charAt(end)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (end - start + <span class="number">1</span> &gt; maxLen) &#123;</span><br><span class="line">            maxLen = end - start + <span class="number">1</span>;</span><br><span class="line">            subString = s.substring(start, end + <span class="number">1</span>);<span class="comment">//区间是 [start, end],截取的时候, end+1</span></span><br><span class="line">        &#125;</span><br><span class="line">        start--;</span><br><span class="line">        end++;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后我们考虑如何用动态规划的方式来解决这个问题，回文串问题一般都包含重复子问题，例如， 我们要求  bacab 是否是 回文串，则 只需要满足两个条件：1. 该串两端的字符 b == b , 2. 该串的子串  aca 是 回文串。</p>
<p>很明显这是一个区间dp的问题，即 判断 从 i 到 j 的子串是否是 回文串。我们可以得到dp table 的定义：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//dp[i][j] 表示 从 i 到 j 的子串是否是 回文串 ， i &lt; j</span></span><br><span class="line"><span class="keyword">boolean</span>[][] dp = <span class="keyword">new</span> <span class="keyword">boolean</span>[n][n];</span><br></pre></td></tr></table></figure>

<p>然后根据上面两个条件可以得出状态转移方程：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> s[i] == s[j] <span class="comment">// 1. 该串两端的字符 b == b</span></span><br><span class="line">    <span class="keyword">if</span> (i + <span class="number">1</span> &lt; j - <span class="number">1</span>)&#123;</span><br><span class="line">        <span class="comment">//但是这里要注意 i + 1 &lt; j - 1 这样 当前串的子串 才能保证 是 满足定义的字符串</span></span><br><span class="line">        dp[i][j] = d[i + <span class="number">1</span>][j - <span class="number">1</span>];</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123; <span class="comment">// 当前串 子串为空  即  当前串为  bb  的情况</span></span><br><span class="line">        dp[i][j] = <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">else</span> <span class="comment">// 该串两端的字符不等</span></span><br><span class="line">    dp[i][j] = <span class="keyword">false</span>;</span><br></pre></td></tr></table></figure>



<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">longestPalindrome</span><span class="params">(String s)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (s == <span class="keyword">null</span> || s.length() &lt;= <span class="number">1</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> s;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//dp[i][j] 表示 从 i 到 j 的子串是否是 回文串</span></span><br><span class="line">        <span class="keyword">char</span>[] chars = s.toCharArray();</span><br><span class="line">        <span class="keyword">int</span> n = chars.length;</span><br><span class="line">        <span class="keyword">boolean</span>[][] dp = <span class="keyword">new</span> <span class="keyword">boolean</span>[n][n];</span><br><span class="line">        <span class="comment">//init</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i =<span class="number">0</span>; i &lt; n; i++)&#123;</span><br><span class="line">            dp[i][i] = <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> maxLen = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">int</span> start = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//dp[i][j] = (s[i] == s[j]) &amp;&amp; dp[i + 1][j - 1]</span></span><br><span class="line">        <span class="comment">//先遍历列,为了 保证 dp[i+1][j-1] 在 dp[i][j] 之前算出来</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">1</span>; j &lt; n; j++)&#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; j; i++)&#123;</span><br><span class="line">                <span class="keyword">if</span> (chars[i] == chars[j])&#123;</span><br><span class="line">                    <span class="keyword">if</span> (i + <span class="number">1</span> &lt; j - <span class="number">1</span>)&#123;      </span><br><span class="line">                <span class="comment">// 可能你有疑问，这里 算 i 时 用到了 i+1 ,那么 i+1 不是还没被算出来吗</span></span><br><span class="line">                <span class="comment">//其实不是，这里 先遍历的列，所以 上一轮的 dp[i + 1][j - 1] 已经算出来了</span></span><br><span class="line">                <span class="comment">// 这一轮的 dp[i][j] 使用上一轮的结果没有问题， 这一轮的 dp[i + 1][j] 确实没有算出来</span></span><br><span class="line">                        dp[i][j] = dp[i + <span class="number">1</span>][j - <span class="number">1</span>];</span><br><span class="line">                    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                        dp[i][j] = <span class="keyword">true</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                    dp[i][j] = <span class="keyword">false</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 找出所有回文中最长的</span></span><br><span class="line">                <span class="keyword">if</span> (dp[i][j] &amp;&amp; j - i + <span class="number">1</span> &gt; maxLen)&#123;</span><br><span class="line">                    maxLen = j - i + <span class="number">1</span>;</span><br><span class="line">                    start = i;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> s.substring(start, start + maxLen);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>



<h4 id="最长回文子序列-longestPalindromeSubseq-Leet-516"><a href="#最长回文子序列-longestPalindromeSubseq-Leet-516" class="headerlink" title="最长回文子序列 longestPalindromeSubseq  Leet  516"></a>最长回文子序列 longestPalindromeSubseq  Leet  516</h4><p>题目：给定一个字符串s，找到其中最长的回文子序列。可以假设s的最大长度为1000。</p>
<p>示例 1:  输入:  “bbbab”<br>             输出:  4<br>             一个可能的最长回文子序列为 “bbbb”。</p>
<p>回文子序列和 回文子串不同， 子序列在原串中 不一定是连续的，如示例所示。回文子序列也包含重复子问题，但是情况要比 回文子串要复杂一点，同样的，我们先来看一个例子，我要找到 串  cbbac 的最长回文子序列，也需要两个条件：1. 当前串的两端的字符是否相同 ，即 c == c , 2. 当前串的子串 bba 的最长回文子序列长度为 多少，这里为 2。那么该串的最长回文子序列长度为  子串的结果（2）+ 2 = 4；</p>
<p>因此，回文子序列问题的 dp table 的定义 与 回文子串的定义一致：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//dp[i][j] 表示 从 i 到 j 的字符串的最长回文子序列的长度</span></span><br><span class="line"><span class="keyword">int</span>[][] dp = <span class="keyword">new</span> <span class="keyword">int</span>[n][n];</span><br></pre></td></tr></table></figure>

<p>那么，如何得到转移方程呢，这里需要讨论：（在所有的状态转移的情况中，关键是要 不漏情况，一些情况可以重复，最好做到 不重不漏）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1. 当 s[i] == s[j]， 这种情况简单, 即子串的结果 加上 2 </span></span><br><span class="line">	dp[i][j] = dp[i + <span class="number">1</span>][j - <span class="number">1</span>] + <span class="number">2</span></span><br><span class="line"><span class="comment">//2. 当 s[i] != s[j]，此时需要比较 三种情况, 例：cbbcd 此时 c != d</span></span><br><span class="line">        <span class="comment">//2.1 dp[i + 1][j] 子串 bbcd 的结果  2</span></span><br><span class="line">        <span class="comment">//2.2 dp[i][j - 1] 子串 cbbc 的结果  4</span></span><br><span class="line">        <span class="comment">//2.3 dp[i - 1][j - 1] 子串 bbc 的结果 2， 这种情况其实包含在了 上述两种情况的结果中了，重复了</span></span><br><span class="line">    dp[i][j] = max(dp[i + <span class="number">1</span>][j], dp[i][j - <span class="number">1</span>]);</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">longestPalindromeSubseq</span><span class="params">(String s)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(s == <span class="keyword">null</span> || s.length() == <span class="number">0</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> n = s.length();</span><br><span class="line">    <span class="comment">//dp[i][j] 表示 从 i 到 j 的字符串的最长回文子序列的长度</span></span><br><span class="line">	<span class="keyword">int</span>[][] dp = <span class="keyword">new</span> <span class="keyword">int</span>[n][n];</span><br><span class="line">   	</span><br><span class="line">    <span class="comment">//base case: 每个子符它本身 的最长回文子序列的长度 为 1 </span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)&#123;</span><br><span class="line">        dp[i][i] = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 遍历 完成状态转移</span></span><br><span class="line">    <span class="comment">//怎么遍历呢，这时要考虑转移方程, dp[i][j] = max(dp[i + 1][j], dp[i][j - 1]);  计算 dp[i][j] 需要 知道dp[i + 1][j]， 则 i 只能倒着遍历，这样才能保证 dp[i + 1][j] 在 dp[i][j] 之前算出来, 此时 j 呢， j&gt; i，则 j 从 i+1 开始， 且 需要 dp[i][j - 1] 在  dp[i][j] 之前算出来 ， j 正着遍历</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = n - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--)&#123;</span><br><span class="line">    	<span class="keyword">for</span>(<span class="keyword">int</span> j = i + <span class="number">1</span>; j &lt; n; j++)&#123;</span><br><span class="line">            <span class="keyword">if</span> (s.charAt(i) == s.charAt(j))&#123;</span><br><span class="line">                <span class="comment">// 这时候就不需要 判断 i + 1 &lt; j - 1， 因为不满足的 一定 是 0</span></span><br><span class="line">                dp[i][j] = dp[i + <span class="number">1</span>][j - <span class="number">1</span>] + <span class="number">2</span>;</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                dp[i][j] = Math.max(dp[i + <span class="number">1</span>][j], dp[i][j - <span class="number">1</span>]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 返回结果，根据 定义 ： 从 0  到 n - 1 的字符串的最长回文子序列的长度</span></span><br><span class="line">    <span class="keyword">return</span> dp[<span class="number">0</span>][n - <span class="number">1</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="最长公共子序列-longestCommonSubsequence-Leet-1143"><a href="#最长公共子序列-longestCommonSubsequence-Leet-1143" class="headerlink" title="最长公共子序列  longestCommonSubsequence   Leet 1143"></a>最长公共子序列  longestCommonSubsequence   Leet 1143</h4><p>题目：给定两个字符串 text1 和 text2，返回这两个字符串的最长公共子序列的长度。</p>
<p>示例 1:  输入：text1 = “abcde”, text2 = “ace”<br>             输出：3<br>             解释：最长公共子序列是 “ace”，它的长度为 3。</p>
<h4 id="编辑距离-minDistance-Leet-72"><a href="#编辑距离-minDistance-Leet-72" class="headerlink" title="编辑距离  minDistance  Leet 72"></a>编辑距离  minDistance  Leet 72</h4><p>题目：给你两个单词 word1 和 word2，请你计算出将 word1 转换成 word2 所使用的最少操作数 。</p>
<p>你可以对一个单词进行如下三种操作：</p>
<p>插入一个字符<br>删除一个字符<br>替换一个字符</p>
<p>示例 1：输入：word1 = “horse”, word2 = “ros”<br>              输出：3<br>              解释：<br>                horse -&gt; rorse (将 ‘h’ 替换为 ‘r’)<br>                rorse -&gt; rose (删除 ‘r’)<br>                rose -&gt; ros (删除 ‘e’)</p>
]]></content>
      <tags>
        <tag>DP</tag>
      </tags>
  </entry>
  <entry>
    <title>ThreadLocal-笔记</title>
    <url>/2020/06/05/ThreadLocal-%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h2 id="TheadLocal-作用，原理，内存泄漏原因分析"><a href="#TheadLocal-作用，原理，内存泄漏原因分析" class="headerlink" title="TheadLocal 作用，原理，内存泄漏原因分析"></a>TheadLocal 作用，原理，内存泄漏原因分析</h2><h3 id="ThreadLocal-是什么"><a href="#ThreadLocal-是什么" class="headerlink" title="ThreadLocal 是什么?"></a>ThreadLocal 是什么?</h3><p>ThreadLocal是线程Thread中属性threadLocals的管理者。Thread中属性threadLocals，作为一个特殊的Map，它的key值就是我们ThreadLocal实例，而value值这是我们设置的值。</p>
<p>Synchronized用于线程间的数据共享，而ThreadLocal则用于线程间的数据隔离。 ThreadLocal提供一个线程（Thread）局部变量，访问到某个变量的每一个线程都拥有自己的局部变量，ThreadLocal就是想在多线程环境下去保证成员变量的安全。ThreadLocal 用于保存某个线程的共享变量，对于同一个static ThreadLocal，不同线程只能从中get，set，remove自己的变量，而不会影响其他线程的变量。（懵）</p>
<a id="more"></a>

<p>上面的定义看完也不知道它具体是干啥的，下面我们来具体分析一下：</p>
<p>首先，ThreadLocal 对象是作为一个线程的 局部变量存在的，具体来说，如下面的例子所示，threadLocal对象 作为当前main 线程的一个局部变量存在，可以通过 set get 方法 保存一个共享变量 （“xi”），这个共享变量在main线程中可以通过threadLocal对象来获取。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadLcoalDemo</span> </span>&#123;</span><br><span class="line">      <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//这个 threadLocal对象 作为当前main 线程的一个局部变量存在，</span></span><br><span class="line">        ThreadLocal&lt;String&gt; threadLocal =  <span class="keyword">new</span> ThreadLocal&lt;&gt;();</span><br><span class="line">        ThreadLocal&lt;String&gt; threadLocal2 =  <span class="keyword">new</span> ThreadLocal&lt;&gt;();</span><br><span class="line">        threadLocal.set(<span class="string">"xi"</span>);</span><br><span class="line">        threadLocal2.set(<span class="string">"ha"</span>);</span><br><span class="line">        System.out.println(threadLocal.get());</span><br><span class="line">        System.out.println(threadLocal2.get());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是，这个 共享变量 “xi” 保存在哪呢，我们可以去看下set 方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(T value)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//得到 当前线程</span></span><br><span class="line">        Thread t = Thread.currentThread();</span><br><span class="line">    <span class="comment">//通过当前线程获得一个 ThreadLocalMap</span></span><br><span class="line">        ThreadLocalMap map = getMap(t);</span><br><span class="line">        <span class="keyword">if</span> (map != <span class="keyword">null</span>)</span><br><span class="line">            map.set(<span class="keyword">this</span>, value);</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            createMap(t, value);</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// getMap 方法</span></span><br><span class="line"><span class="function">ThreadLocalMap <span class="title">getMap</span><span class="params">(Thread t)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> t.threadLocals;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>set方法中通过当前线程的引用 t 得到了一个 ThreadLocalMap ，这个 ThreadLocalMap 是从哪来的呢，原来，Thread类中有一个成员变量 threadLocals，如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Thread</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line"><span class="comment">/* ThreadLocal values pertaining to this thread. This map is maintained</span></span><br><span class="line"><span class="comment">     * by the ThreadLocal class. */</span></span><br><span class="line">    ThreadLocal.ThreadLocalMap threadLocals = <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个 threadLocals 的类型 就是 ThreadLocalMap ，默认为null，所以第一次通过 threadLocal 的set 方法调用时，就通过creatMap() 方法初始化，然后将 当前 threadLocal对象 作为key，共享变量 “xi” 作为value ，存入这个 threadLocals 这个 map里面了。threadLocals 这个map里面 就保存了所有的 这样的 键值对，用于在当前线程内部共享变量。</p>
<h3 id="ThreadLocal-原理"><a href="#ThreadLocal-原理" class="headerlink" title="ThreadLocal 原理"></a>ThreadLocal 原理</h3><p><img src="/2020/06/05/ThreadLocal-%E7%AC%94%E8%AE%B0/image-threadlocal.png" alt="image-20200606162418863"></p>
<p>根据依赖关系，ThreadLocalMap 为 ThreadLocal 的静态内部类，Entry 为 ThreadLocalMap 的静态内部类。ThreadLocalMap 的实现与 HashMap 有很大不同，首先，ThreadLocalMap 中的 Entry  继承了 WeakReference，以 ThreadLocal 作为 key 的这个key 是一个弱引用 ，弱引用当GC发生的时候，定义的ThreadLocal是有可能被回收的，即key为null，value 还通过一条强引用链被引用，这可能会导致之后的内存泄漏的发生。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Entry</span> <span class="keyword">extends</span> <span class="title">WeakReference</span>&lt;<span class="title">ThreadLocal</span>&lt;?&gt;&gt; </span>&#123;</span><br><span class="line">           <span class="comment">/** The value associated with this ThreadLocal. */</span></span><br><span class="line">           Object value;</span><br><span class="line">           Entry(ThreadLocal&lt;?&gt; k, Object v) &#123;</span><br><span class="line">               <span class="keyword">super</span>(k);<span class="comment">// key 弱引用</span></span><br><span class="line">               value = v;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">createMap</span><span class="params">(Thread t, T firstValue)</span> </span>&#123;</span><br><span class="line">        t.threadLocals = <span class="keyword">new</span> ThreadLocalMap(<span class="keyword">this</span>, firstValue);</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// ThreadLocalMap 的构造器 </span></span><br><span class="line">ThreadLocalMap(ThreadLocal&lt;?&gt; firstKey, Object firstValue) &#123;</span><br><span class="line">            table = <span class="keyword">new</span> Entry[INITIAL_CAPACITY];</span><br><span class="line">    <span class="comment">// threadLocalHashCode 是用来唯一确定一个ThreadLocal对象</span></span><br><span class="line">    <span class="comment">// 每初始化一个ThreadLocal对象，hash值就增加一个固定的HASH_INCREMENT(0x61c88647)大小</span></span><br><span class="line">    <span class="comment">//通过 原子类 AtomicInteger 来 自增的 ，保证 唯一性</span></span><br><span class="line">            <span class="keyword">int</span> i = firstKey.threadLocalHashCode &amp; (INITIAL_CAPACITY - <span class="number">1</span>);</span><br><span class="line">            table[i] = <span class="keyword">new</span> Entry(firstKey, firstValue);</span><br><span class="line">            size = <span class="number">1</span>;</span><br><span class="line">            setThreshold(INITIAL_CAPACITY);</span><br><span class="line">        &#125;</span><br><span class="line"> <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">setThreshold</span><span class="params">(<span class="keyword">int</span> len)</span> </span>&#123;</span><br><span class="line">            threshold = len * <span class="number">2</span> / <span class="number">3</span>; <span class="comment">//负载因子为 2/3 与 HashMap的 0.75 不同</span></span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<p>由于保证了ThreadLocal对象 的唯一性，所以 可以用不同的ThreadLocal作为key，区分不同的value，方便存取。</p>
<p>这里用来解决Hash冲突的方法与HashMap 不同，HashMap 俗称拉链法，就是用一个桶位，链表或红黑树把Hash值相同的元素串起来。但是ThreadLocalMap使用的方法称为 <strong>线性探查法</strong>， 这种方法的特点是：冲突发生时，顺序查看表中下一单元，直到找出一个空单元或查遍全表。 </p>
<p>所谓线性探查，就是根据初始 key 的 hashcode 值确定元素在 table 数组中的位置，如果发现这个位置上已经被其他的 key 值占用，则利用固定的算法寻找一定步长的下个位置，依次判断，直至找到能够存放的位置。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(ThreadLocal&lt;?&gt; key, Object value)</span> </span>&#123;</span><br><span class="line">            <span class="comment">// We don't use a fast path as with get() because it is at</span></span><br><span class="line">            <span class="comment">// least as common to use set() to create new entries as</span></span><br><span class="line">            <span class="comment">// it is to replace existing ones, in which case, a fast</span></span><br><span class="line">            <span class="comment">// path would fail more often than not.</span></span><br><span class="line"></span><br><span class="line">            Entry[] tab = table;</span><br><span class="line">            <span class="keyword">int</span> len = tab.length;</span><br><span class="line">            <span class="keyword">int</span> i = key.threadLocalHashCode &amp; (len-<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">for</span> (Entry e = tab[i];</span><br><span class="line">                 e != <span class="keyword">null</span>;</span><br><span class="line">                 e = tab[i = nextIndex(i, len)]) &#123;</span><br><span class="line">                ThreadLocal&lt;?&gt; k = e.get();</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (k == key) &#123;</span><br><span class="line">                    e.value = value;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (k == <span class="keyword">null</span>) &#123;</span><br><span class="line">                    replaceStaleEntry(key, value, i);</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            tab[i] = <span class="keyword">new</span> Entry(key, value);</span><br><span class="line">            <span class="keyword">int</span> sz = ++size;</span><br><span class="line">            <span class="keyword">if</span> (!cleanSomeSlots(i, sz) &amp;&amp; sz &gt;= threshold)</span><br><span class="line">                rehash();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">nextIndex</span><span class="params">(<span class="keyword">int</span> i, <span class="keyword">int</span> len)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> ((i + <span class="number">1</span> &lt; len) ? i + <span class="number">1</span> : <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">prevIndex</span><span class="params">(<span class="keyword">int</span> i, <span class="keyword">int</span> len)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> ((i - <span class="number">1</span> &gt;= <span class="number">0</span>) ? i - <span class="number">1</span> : len - <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>ThreadLocalMap解决Hash冲突的方式就是简单的步长加1或减1，寻找下一个相邻的位置。 在插入过程中，根据ThreadLocal对象的hash值，定位到table中的位置 i，过程如下：</p>
<ol>
<li>如果当前位置是空的，那么正好，就初始化一个Entry对象放在位置i上</li>
<li>位置i已有对象，如果这个Entry对象的key正好是即将设置的key，那么覆盖value </li>
<li>位置i的对象，和即将设置的key没关系，那么只能找下一个空位置 </li>
</ol>
<h3 id="ThreadLocal-可能造成内存泄漏"><a href="#ThreadLocal-可能造成内存泄漏" class="headerlink" title="ThreadLocal 可能造成内存泄漏"></a>ThreadLocal 可能造成内存泄漏</h3><p>Memory leak:内存泄漏，程序申请内存后，无法释放已申请的内存空间，内存泄漏的堆积终将导致内存溢出。<br> 为什么说TreadLocal会引起内存泄漏 ，显然是TreadLocal在不规范使用的情况下导致了内存没有释放。 </p>
<p>由上面 ThreadLocalMap 的内部类 Entry 的代码所示， Entry 继承了 WeakReference，所以它的 key （ThreadLocal 对象）是弱引用，而 value （变量副本）是强引用。</p>
<p>如果 ThreadLocal 对象没有外部强引用来引用它，那么 ThreadLocal 对象会在下次 GC 时被回收。此时，Entry 中的 key 已经被回收，但是 value 由于是强引用不会被垃圾收集器回收。如果创建 ThreadLocal 的线程一直持续运行，那么 value 就会一直得不到回收，产生内存泄漏。</p>
<p>ThreadLocal产生内存泄漏的条件：</p>
<ol>
<li>ThreadLocal引用被设置为null，且后面没有set，get,remove操作。</li>
<li>线程一直运行，不停止。</li>
<li>触发了垃圾回收。</li>
</ol>
<p>避免内存泄漏的方法：</p>
<ol>
<li><p>ThreadLocal申明为private static final。private 与 final 尽可能不让他人修改变更引用， static 表示为类属性，只有在程序结束才会被回收。 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> ThreadLocal&lt;String&gt; threadLocal =  <span class="keyword">new</span> ThreadLocal&lt;&gt;(); <span class="comment">//一个threadLocal 可以管理多个 线程 中的 ThreadLocalMap</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">      threadLocal.set(<span class="string">"I am  main"</span>);</span><br><span class="line">      System.out.println(threadLocal.get());</span><br><span class="line">     </span><br><span class="line">      <span class="keyword">new</span> Thread(() -&gt; threadLocal.set(<span class="string">"I am A"</span>),<span class="string">"A"</span>).start();</span><br><span class="line">   </span><br><span class="line">      <span class="keyword">new</span> Thread(() -&gt; threadLocal.set(<span class="string">"I am B"</span>),<span class="string">"B"</span>).start();</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>使用 ThreadLocal 的 set 方法后，显示的调用 remove 方法。  ThreadLocal使用后务必调用remove方法。</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ThreadLocal&lt;String&gt; threadLocal = <span class="keyword">new</span> ThreadLocal();</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    threadLocal.set(<span class="string">"xxx"</span>);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">    threadLocal.remove();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>ThreadLocal</tag>
      </tags>
  </entry>
  <entry>
    <title>HashMap 笔记</title>
    <url>/2020/05/21/HashMap-%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h3 id="HashMap-的-put-方法"><a href="#HashMap-的-put-方法" class="headerlink" title="HashMap 的 put 方法"></a>HashMap 的 put 方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">put</span><span class="params">(K key, V value)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> putVal(hash(key), key, value, <span class="keyword">false</span>, <span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>当我们调用put方法时，首先对传入的 key 进行 hash(key) 操作，这是一个扰动函数，具体作用是让key的hashcode的高16位也参与运算。以减少hash冲突的概率。从中也可以看出，key 为null的值默认的存放位置为 0 。</p>
<a id="more"></a>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; <span class="keyword">implements</span> <span class="title">Map</span>.<span class="title">Entry</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> <span class="keyword">int</span> hash;</span><br><span class="line">        <span class="keyword">final</span> K key;</span><br><span class="line">        V value;</span><br><span class="line">        Node&lt;K,V&gt; next;</span><br><span class="line">    </span><br><span class="line">    	......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">  <span class="comment">// 例 ：h = 0b 0100 0101 1001 0010 1011 0001 1011 1000</span></span><br><span class="line">    <span class="comment">// 0b 0100 0101 1001 0010 1011 0001 1011 1000 </span></span><br><span class="line">    <span class="comment">// ^</span></span><br><span class="line">    <span class="comment">// 0b 0000 0000 0000 0000 0100 0101 1001 0010</span></span><br><span class="line">    <span class="comment">// = 0b 0100 0101 1001 0010 1111 0100 0010 1010</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">hash</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> h;</span><br><span class="line">        <span class="keyword">return</span> (key == <span class="keyword">null</span>) ? <span class="number">0</span> : (h = key.hashCode()) ^ (h &gt;&gt;&gt; <span class="number">16</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> V <span class="title">putVal</span><span class="params">(<span class="keyword">int</span> hash, K key, V value, <span class="keyword">boolean</span> onlyIfAbsent, <span class="keyword">boolean</span> evict)</span> </span>&#123;...&#125;</span><br></pre></td></tr></table></figure>

<p>onlyIfAbsent 参数 为true时，当传入的key存在时，不执行插入替换操作，一般为false，有则替换，没有就插入。evict </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> V <span class="title">putVal</span><span class="params">(<span class="keyword">int</span> hash, K key, V value, <span class="keyword">boolean</span> onlyIfAbsent,</span></span></span><br><span class="line"><span class="function"><span class="params">                   <span class="keyword">boolean</span> evict)</span> </span>&#123;</span><br><span class="line">    	<span class="comment">//tab: 引用当前hashmap的散列表，就是一个node数组的引用</span></span><br><span class="line">    	<span class="comment">//p: 表示当前散列表的元素</span></span><br><span class="line">    	<span class="comment">//n: 表示当前Node数组的长度</span></span><br><span class="line">    	<span class="comment">//i: 表示路由寻址的结果</span></span><br><span class="line">        Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; <span class="keyword">int</span> n, i;</span><br><span class="line">    	<span class="comment">//延迟初始化，第一次调用putVal方法时初始化HashMap中最占内存的 Node 数组</span></span><br><span class="line">    	<span class="comment">//初始化也是通过 resize() 方法完成的</span></span><br><span class="line">    	<span class="comment">//这里也完成了对 n 的赋值，即 hashMap 的 capacity</span></span><br><span class="line">        <span class="keyword">if</span> ((tab = table) == <span class="keyword">null</span> || (n = tab.length) == <span class="number">0</span>)</span><br><span class="line">            n = (tab = resize()).length;</span><br><span class="line">    	<span class="comment">// 根据路由算法 (n - 1) &amp; hash 计算出数组中要插入的位置下标</span></span><br><span class="line">    	<span class="comment">//最简单的情况，寻址找到的桶位刚好为null，直接将当前 k-v ==&gt; node 放入</span></span><br><span class="line">        <span class="keyword">if</span> ((p = tab[i = (n - <span class="number">1</span>) &amp; hash]) == <span class="keyword">null</span>)</span><br><span class="line">            tab[i] = newNode(hash, key, value, <span class="keyword">null</span>);</span><br><span class="line">    	<span class="comment">// 寻址找到的桶位不为null</span></span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// e：不为null，表明找到了一个与当前key-value 的 key 相同的node，便于之后替换value</span></span><br><span class="line">            <span class="comment">// k: 临时变量 key</span></span><br><span class="line">            Node&lt;K,V&gt; e; K k;</span><br><span class="line">            <span class="comment">//表示该桶位上的元素的key 和当前要插入的元素的key相同，表示要进行替换，就赋给e</span></span><br><span class="line">            <span class="keyword">if</span> (p.hash == hash &amp;&amp;</span><br><span class="line">                ((k = p.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                e = p;</span><br><span class="line">            <span class="comment">//表示当前桶位上的元素为 树节点，就执行树的插入操作</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (p <span class="keyword">instanceof</span> TreeNode) <span class="comment">// true 表示 p 为一颗红黑树</span></span><br><span class="line">                e = ((TreeNode&lt;K,V&gt;)p).putTreeVal(<span class="keyword">this</span>, tab, hash, key, value);</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">//当前桶位上有一个链表的情况，而且链表的头元素的key 与 当前要插入的元素的key 不同</span></span><br><span class="line">                <span class="comment">//遍历这个链表</span></span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> binCount = <span class="number">0</span>; ; ++binCount) &#123;</span><br><span class="line">                    <span class="comment">//当 到达链表的尾部时，也没有找到一个key值和当前要插入的元素的key相同的元素</span></span><br><span class="line">                    <span class="comment">//插入当前元素到链表尾部， jdk1.8 为 尾插法。</span></span><br><span class="line">                    <span class="keyword">if</span> ((e = p.next) == <span class="keyword">null</span>) &#123;</span><br><span class="line">                        p.next = newNode(hash, key, value, <span class="keyword">null</span>);</span><br><span class="line">                        <span class="comment">// 达到了树化条件，将链表转化为树</span></span><br><span class="line">                        <span class="keyword">if</span> (binCount &gt;= TREEIFY_THRESHOLD - <span class="number">1</span>) <span class="comment">// -1 for 1st</span></span><br><span class="line">                            treeifyBin(tab, hash);</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">//条件成立，说明在链表中找到了一个key值和当前要插入的元素的key相同的元素</span></span><br><span class="line">                    <span class="comment">//跳出，以便后面进行替换 value 的操作</span></span><br><span class="line">                    <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                        ((k = e.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    <span class="comment">// 由于上面 (e = p.next) 的时候已经赋值next 给e 了，此时直接将e赋给p，表示向后移动</span></span><br><span class="line">                    p = e;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//e 不为null 表示要进行替换，并返回 旧值</span></span><br><span class="line">            <span class="keyword">if</span> (e != <span class="keyword">null</span>) &#123; <span class="comment">// existing mapping for key</span></span><br><span class="line">                V oldValue = e.value;</span><br><span class="line">                <span class="keyword">if</span> (!onlyIfAbsent || oldValue == <span class="keyword">null</span>)</span><br><span class="line">                    e.value = value;</span><br><span class="line">                afterNodeAccess(e);</span><br><span class="line">                <span class="keyword">return</span> oldValue;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    	<span class="comment">//modCount 表示 散列表结构修改的次数， 替换 Node 元素的value 操作不算,</span></span><br><span class="line">    	<span class="comment">//因为如果是替换的话，上面已经返回了，不会执行到这。</span></span><br><span class="line">        ++modCount;</span><br><span class="line">    	<span class="comment">//当前大小加1， ++size ，如果大于扩容阈值，就扩容</span></span><br><span class="line">        <span class="keyword">if</span> (++size &gt; threshold) </span><br><span class="line">            resize();</span><br><span class="line">        afterNodeInsertion(evict);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="HashMap-的-resize-方法"><a href="#HashMap-的-resize-方法" class="headerlink" title="HashMap 的 resize 方法"></a>HashMap 的 resize 方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> Node&lt;K,V&gt;[] resize() &#123;</span><br><span class="line">    <span class="comment">//oldTab：旧 Node 数组的引用</span></span><br><span class="line">    <span class="comment">//oldCap: 旧的数组的长度 即旧的容量</span></span><br><span class="line">    <span class="comment">//oldThr: 旧的 扩容阈值</span></span><br><span class="line">       Node&lt;K,V&gt;[] oldTab = table;</span><br><span class="line">       <span class="keyword">int</span> oldCap = (oldTab == <span class="keyword">null</span>) ? <span class="number">0</span> : oldTab.length;</span><br><span class="line">       <span class="keyword">int</span> oldThr = threshold;</span><br><span class="line">       <span class="keyword">int</span> newCap, newThr = <span class="number">0</span>;</span><br><span class="line">       <span class="keyword">if</span> (oldCap &gt; <span class="number">0</span>) &#123;</span><br><span class="line">           <span class="comment">// 最大容量不得超过 Integer.MAX_VALUE</span></span><br><span class="line">           <span class="keyword">if</span> (oldCap &gt;= MAXIMUM_CAPACITY) &#123;</span><br><span class="line">               threshold = Integer.MAX_VALUE;</span><br><span class="line">               <span class="keyword">return</span> oldTab;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="comment">// newCap 扩容为 原来的 两倍 oldCap &lt;&lt; 1 左移1位 == *2</span></span><br><span class="line">           <span class="keyword">else</span> <span class="keyword">if</span> ((newCap = oldCap &lt;&lt; <span class="number">1</span>) &lt; MAXIMUM_CAPACITY &amp;&amp;</span><br><span class="line">                    oldCap &gt;= DEFAULT_INITIAL_CAPACITY)</span><br><span class="line">               <span class="comment">// 扩容阈值 也翻倍 </span></span><br><span class="line">               newThr = oldThr &lt;&lt; <span class="number">1</span>; <span class="comment">// double threshold</span></span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">else</span> <span class="keyword">if</span> (oldThr &gt; <span class="number">0</span>) <span class="comment">// initial capacity was placed in threshold</span></span><br><span class="line">           newCap = oldThr;</span><br><span class="line">    <span class="comment">// 首次初始化时 newCap = 16 ， newThr = 0.75 * 16</span></span><br><span class="line">       <span class="keyword">else</span> &#123;               <span class="comment">// zero initial threshold signifies using defaults</span></span><br><span class="line">           newCap = DEFAULT_INITIAL_CAPACITY;</span><br><span class="line">           newThr = (<span class="keyword">int</span>)(DEFAULT_LOAD_FACTOR * DEFAULT_INITIAL_CAPACITY);</span><br><span class="line">       &#125;</span><br><span class="line">    </span><br><span class="line">       <span class="keyword">if</span> (newThr == <span class="number">0</span>) &#123;</span><br><span class="line">           <span class="keyword">float</span> ft = (<span class="keyword">float</span>)newCap * loadFactor;</span><br><span class="line">           newThr = (newCap &lt; MAXIMUM_CAPACITY &amp;&amp; ft &lt; (<span class="keyword">float</span>)MAXIMUM_CAPACITY ?</span><br><span class="line">                     (<span class="keyword">int</span>)ft : Integer.MAX_VALUE);</span><br><span class="line">       &#125;</span><br><span class="line">    <span class="comment">// 更新扩容阈值</span></span><br><span class="line">       threshold = newThr;</span><br><span class="line">       <span class="meta">@SuppressWarnings</span>(&#123;<span class="string">"rawtypes"</span>,<span class="string">"unchecked"</span>&#125;)</span><br><span class="line">    <span class="comment">// 这里就 创建了一个 大小为 newCap 的新数组</span></span><br><span class="line">           Node&lt;K,V&gt;[] newTab = (Node&lt;K,V&gt;[])<span class="keyword">new</span> Node[newCap];</span><br><span class="line">    <span class="comment">//更新 数组</span></span><br><span class="line">       table = newTab;</span><br><span class="line">    <span class="comment">// 当 旧数组不为空  就进行数据迁移操作</span></span><br><span class="line">       <span class="keyword">if</span> (oldTab != <span class="keyword">null</span>) &#123;</span><br><span class="line">           <span class="comment">//遍历旧数组</span></span><br><span class="line">           <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; oldCap; ++j) &#123;</span><br><span class="line">               Node&lt;K,V&gt; e;</span><br><span class="line">               <span class="keyword">if</span> ((e = oldTab[j]) != <span class="keyword">null</span>) &#123;</span><br><span class="line">                   oldTab[j] = <span class="keyword">null</span>; <span class="comment">// 便于垃圾回收</span></span><br><span class="line">                   <span class="keyword">if</span> (e.next == <span class="keyword">null</span>) <span class="comment">//1：该桶位中 只有一个元素 </span></span><br><span class="line">                       <span class="comment">// 将hash值 与 （新的大小 - 1） 相与 得到 在新数组中的位置，放入即可</span></span><br><span class="line">                       newTab[e.hash &amp; (newCap - <span class="number">1</span>)] = e;</span><br><span class="line">                   <span class="keyword">else</span> <span class="keyword">if</span> (e <span class="keyword">instanceof</span> TreeNode)<span class="comment">//2： 是红黑树的话 进行红黑树相应操作</span></span><br><span class="line">                       ((TreeNode&lt;K,V&gt;)e).split(<span class="keyword">this</span>, newTab, j, oldCap);</span><br><span class="line">                   <span class="keyword">else</span> &#123; <span class="comment">// preserve order</span></span><br><span class="line">                       <span class="comment">//3：该桶位中有一个链表 </span></span><br><span class="line">                       Node&lt;K,V&gt; loHead = <span class="keyword">null</span>, loTail = <span class="keyword">null</span>; <span class="comment">// 低位头节点，低位尾节点</span></span><br><span class="line">                       Node&lt;K,V&gt; hiHead = <span class="keyword">null</span>, hiTail = <span class="keyword">null</span>; <span class="comment">// 高位头节点，高位尾节点</span></span><br><span class="line">                       Node&lt;K,V&gt; next;</span><br><span class="line">                       <span class="comment">// 通过这个 do-while 操作 ，就将原来在同一个桶位中的元素 ，分开放到了 新数组中的</span></span><br><span class="line">                       <span class="comment">//高位桶  和低位桶中</span></span><br><span class="line">                       <span class="keyword">do</span> &#123;</span><br><span class="line">                           next = e.next;</span><br><span class="line">                           <span class="comment">// 例： e.hash = 0000 0000 0001 0101 和 e.hash = 0000 0000 0000 0101</span></span><br><span class="line">                           <span class="comment">// 这两个 key的哈希不同， 但是， 在旧 数组中，他们放在同一个桶位</span></span><br><span class="line">                           <span class="comment">// 即  0001 0101 &amp; 0000 1111 = 0000 0101 &amp; 0000 1111 = 0000 0101 = 5</span></span><br><span class="line">                           <span class="comment">//都放在下标为 5 的位置</span></span><br><span class="line">                           <span class="comment">// 通过 (e.hash &amp; oldCap) 操作 假设 oldCap = 16 = 0001 0000</span></span><br><span class="line">                           <span class="comment">// 第一个： 0001 0101 &amp; 0001 0000  != 0</span></span><br><span class="line">                           <span class="comment">// 第二个： 0000 0101 &amp; 0001 0000  == 0</span></span><br><span class="line">                           <span class="keyword">if</span> ((e.hash &amp; oldCap) == <span class="number">0</span>) &#123;</span><br><span class="line">                               <span class="comment">// 这里就 将 第二个放在 低位桶的那个链表中</span></span><br><span class="line">                               <span class="keyword">if</span> (loTail == <span class="keyword">null</span>)</span><br><span class="line">                                   loHead = e;</span><br><span class="line">                               <span class="keyword">else</span></span><br><span class="line">                                   loTail.next = e;</span><br><span class="line">                               loTail = e;</span><br><span class="line">                           &#125;</span><br><span class="line">                           <span class="keyword">else</span> &#123;</span><br><span class="line">                               <span class="comment">// 这里就 将 第一个放在 高位桶的那个链表中</span></span><br><span class="line">                               <span class="keyword">if</span> (hiTail == <span class="keyword">null</span>)</span><br><span class="line">                                   hiHead = e;</span><br><span class="line">                               <span class="keyword">else</span></span><br><span class="line">                                   hiTail.next = e;</span><br><span class="line">                               hiTail = e;</span><br><span class="line">                           &#125;</span><br><span class="line">                       &#125; <span class="keyword">while</span> ((e = next) != <span class="keyword">null</span>);</span><br><span class="line">                       <span class="comment">// 低位桶 的下标 与 原来数组中的元素下标一致 ，这里为 0101 = 5 </span></span><br><span class="line">                       <span class="keyword">if</span> (loTail != <span class="keyword">null</span>) &#123;</span><br><span class="line">                           loTail.next = <span class="keyword">null</span>;</span><br><span class="line">                           newTab[j] = loHead;</span><br><span class="line">                       &#125;</span><br><span class="line">                       <span class="comment">// 高位桶 的下标 为 原来数组中的元素下标 + oldCap  = 5 + 16 = 21 这里为 21</span></span><br><span class="line">                       <span class="comment">// 这样就减小了 同一个桶位中 链表的长度</span></span><br><span class="line">                       <span class="keyword">if</span> (hiTail != <span class="keyword">null</span>) &#123;</span><br><span class="line">                           hiTail.next = <span class="keyword">null</span>;</span><br><span class="line">                           newTab[j + oldCap] = hiHead;</span><br><span class="line">                       &#125;</span><br><span class="line">                   &#125;</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">return</span> newTab;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<h3 id="HashMap-的-get-方法"><a href="#HashMap-的-get-方法" class="headerlink" title="HashMap 的 get 方法"></a>HashMap 的 get 方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">get</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">        Node&lt;K,V&gt; e;</span><br><span class="line">    <span class="comment">//get 元素时，也先对 key 进行 hash 操作 ，包括扰动</span></span><br><span class="line">        <span class="keyword">return</span> (e = getNode(hash(key), key)) == <span class="keyword">null</span> ? <span class="keyword">null</span> : e.value;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">//getNode() 方法</span></span><br><span class="line"><span class="function"><span class="keyword">final</span> Node&lt;K,V&gt; <span class="title">getNode</span><span class="params">(<span class="keyword">int</span> hash, Object key)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// tab : Node 数组引用</span></span><br><span class="line">    <span class="comment">// first ： 桶位中的第一个元素</span></span><br><span class="line">    <span class="comment">// e: 桶中元素的临时引用</span></span><br><span class="line">    <span class="comment">// k: 键的 引用 </span></span><br><span class="line">    <span class="comment">// n: 当前长度   默认 16 </span></span><br><span class="line">        Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; first, e; <span class="keyword">int</span> n; K k;</span><br><span class="line">        <span class="keyword">if</span> ((tab = table) != <span class="keyword">null</span> &amp;&amp; (n = tab.length) &gt; <span class="number">0</span> &amp;&amp;</span><br><span class="line">            <span class="comment">// (n - 1) &amp; hash 进行寻址  </span></span><br><span class="line">            (first = tab[(n - <span class="number">1</span>) &amp; hash]) != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 桶位中第一个元素的key 和 当前传入的key 相等 返回 first</span></span><br><span class="line">            <span class="keyword">if</span> (first.hash == hash &amp;&amp; <span class="comment">// always check first node</span></span><br><span class="line">                ((k = first.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                <span class="keyword">return</span> first;</span><br><span class="line">            <span class="comment">// 桶位中 不止一个元素，且 桶位中第一个元素的key 和 当前传入的key 不同  </span></span><br><span class="line">            <span class="keyword">if</span> ((e = first.next) != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (first <span class="keyword">instanceof</span> TreeNode) <span class="comment">// 红黑树，则执行红黑树的查找操作</span></span><br><span class="line">                    <span class="keyword">return</span> ((TreeNode&lt;K,V&gt;)first).getTreeNode(hash, key);</span><br><span class="line">                <span class="comment">// 链表情况，一直向后找，直到找到 key 相同的元素 返回</span></span><br><span class="line">                <span class="keyword">do</span> &#123;</span><br><span class="line">                    <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                        ((k = e.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                        <span class="keyword">return</span> e;</span><br><span class="line">                &#125; <span class="keyword">while</span> ((e = e.next) != <span class="keyword">null</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    <span class="comment">//未找到 返回 null</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>



]]></content>
      <tags>
        <tag>HashMap</tag>
      </tags>
  </entry>
  <entry>
    <title>面试中的智力题</title>
    <url>/2020/06/21/%E9%9D%A2%E8%AF%95%E4%B8%AD%E7%9A%84%E6%99%BA%E5%8A%9B%E9%A2%98/</url>
    <content><![CDATA[<h2 id="智力题"><a href="#智力题" class="headerlink" title="智力题"></a>智力题</h2><h3 id="网易：-Math-round-函数"><a href="#网易：-Math-round-函数" class="headerlink" title="网易： Math.round() 函数"></a>网易： Math.round() 函数</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">round() ：返回四舍五入，负 .5 小数返回较大整数，如 -1.5 返回 -1。</span><br><span class="line">四舍五入的原理是在参数上加0.5然后做向下取整。  例  -1.6 +0.5 &#x3D; -1.1 向下取整 为 -2</span><br><span class="line">ceil() ：返回小数所在两整数间的较大值，如 -1.5 返回 -1。</span><br><span class="line">floor() ：返回小数所在两整数间的较小值，如 -1.5 返回 -2。</span><br><span class="line">floor就是取地板，ceil取天花板，round的实现即时 +0.5然后计算floor</span><br><span class="line">System.out.println(Math.round(1.5));  &#x2F;&#x2F;  2 四舍五入</span><br><span class="line">System.out.println(Math.round(-1.5));  &#x2F;&#x2F;  -1 四舍五入</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<h3 id="网易：问题1：卡特兰数-通项公式："><a href="#网易：问题1：卡特兰数-通项公式：" class="headerlink" title="网易：问题1：卡特兰数 通项公式："></a>网易：<strong>问题1：</strong>卡特兰数 通项公式：<img src="/2020/06/21/%E9%9D%A2%E8%AF%95%E4%B8%AD%E7%9A%84%E6%99%BA%E5%8A%9B%E9%A2%98/%E5%8D%A1%E7%89%B9%E5%85%B0%E6%95%B0%E9%80%9A%E9%A1%B9%E5%85%AC%E5%BC%8F.png" alt></h3><p>例如 ： n = 3  得   Catalan   =  20 / 4  =  5 </p>
<p>卡特兰数递推公式：<img src="/2020/06/21/%E9%9D%A2%E8%AF%95%E4%B8%AD%E7%9A%84%E6%99%BA%E5%8A%9B%E9%A2%98/%E5%8D%A1%E7%89%B9%E5%85%B0%E6%95%B0.png" alt="image-20200910193749562"></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">CatalanNumber</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (n == <span class="number">1</span> || n == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    </span><br><span class="line">        <span class="keyword">return</span> CatalanNumber(n - <span class="number">1</span>) * (<span class="number">4</span> * n - <span class="number">2</span>) / (n + <span class="number">1</span>);      </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>卡特兰数的应用： </p>
<p>1 栈中有n个元素 ，问 出栈的方式有多少种？ 例： abc  三个元素的 出栈方式有5种</p>
<p>2  给定一个整数 <em>n</em>，求以 1 … <em>n</em> 为节点组成的二叉搜索树有多少种？  或者，有N个节点的二叉树共有多少种情形？</p>
<p>3 <strong>找零钱（找一半）</strong>：有2n个人排成一行进入剧场。入场费5元。其中只有n个人有一张5元钞票，另外n人只有10元钞票，剧院无其它钞票，问有多少种方法使得只要有10元的人买票，售票处就有5元的钞票找零？</p>
<p>4 <strong>三角网格</strong></p>
<p><img src="/2020/06/21/%E9%9D%A2%E8%AF%95%E4%B8%AD%E7%9A%84%E6%99%BA%E5%8A%9B%E9%A2%98/%E5%8D%A1%E7%89%B9%E5%85%B0%E6%95%B0%E5%BA%94%E7%94%A84.png" alt="img"></p>
<p>形如这样的直角三角形网格，从左上角开始，只能向右走和向下走，问总共有多少种走法？</p>
<p>5  有n对括号，可以并列或嵌套排列，共有多少种情况？ </p>
<p>6 球分两种颜色，黑色和白色分别各有n只，盒子数量和球的个数相同，每个盒子里面只能放一只球，并且必须满足如下限制,即每一个白球必须和一只黑球配对（白球在黑球前，<strong>允许嵌套</strong>）。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">例.用0表示白球，1表示黑球，则：</span><br><span class="line"></span><br><span class="line">0011，010101，001011合法。</span><br><span class="line"></span><br><span class="line">1100，101010，010110不合法。</span><br></pre></td></tr></table></figure>

<p>7  n个0和n个1组成一个2n位的2进制数，要求从左到右扫描时，1的累计数始终都<strong>小于等于</strong>0的累计数，求满足条件的数有多少？ </p>
<p>8 将多边行划分为三角形问题。将一个<a href="https://baike.baidu.com/item/凸多边形" target="_blank" rel="noopener">凸多边形</a>区域分成三角形区域的方法数?</p>
<p>9 一位大城市的律师在她住所以北n个街区和以东n个街区处工作。每天她走2n个街区去上班。如果他从不穿越（但可以碰到）从家到办公室的对角线，那么有多少条可能的道路？</p>
<p>10 在<a href="https://baike.baidu.com/item/圆上" target="_blank" rel="noopener">圆上</a>选择2n个点,将这些点成对连接起来使得所得到的n条线段不相交的方法数?</p>
<h3 id="网易：问题2：-时针与分针之间的夹角公式：-θ-6Y-30X-0-5Y-5-5Y-30X-单位是度（°）；"><a href="#网易：问题2：-时针与分针之间的夹角公式：-θ-6Y-30X-0-5Y-5-5Y-30X-单位是度（°）；" class="headerlink" title="网易：问题2： 时针与分针之间的夹角公式： θ=|6Y-(30X+0.5Y)|=|5.5Y-30X|,单位是度（°）；"></a>网易：问题2： 时针与分针之间的夹角公式： θ=|6Y-(30X+0.5Y)|=|5.5Y-30X|,单位是度（°）；</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">因为分针每分钟转360&#x2F;60＝6度,</span><br><span class="line">时针每分钟转360&#x2F;（12*60）＝0.5度,</span><br><span class="line">时针每1小时转360&#x2F;12＝30度,</span><br><span class="line">所以,</span><br><span class="line">在X时Y分时,时针与0度起点线的夹角（转过角）是：30X＋0.5Y,</span><br><span class="line">在X时Y分时,分针与0度起点线的夹角（转过角）是：6Y,</span><br><span class="line">时针和分针夹角 θ的计算公式是：  θ&#x3D;|6Y-(30X+0.5Y)|&#x3D;|5.5Y-30X|</span><br></pre></td></tr></table></figure>

<p>例如： 07：55   之间的夹角为  5.5 *55 - 30 * 7 = 92.5 度</p>
<h3 id="字节：设屋里有x人-那么如果3个人一桌-多2个人-如果5个人一桌-多4个人-如果7个人一桌-多6个人-如果9个人一桌-多8个人-如果11个人一桌-正好"><a href="#字节：设屋里有x人-那么如果3个人一桌-多2个人-如果5个人一桌-多4个人-如果7个人一桌-多6个人-如果9个人一桌-多8个人-如果11个人一桌-正好" class="headerlink" title="字节：设屋里有x人,那么如果3个人一桌,多2个人.如果5个人一桌,多4个人.如果7个人一桌,多6个人.如果9个人一桌,多8个人.如果11个人一桌,正好."></a>字节：设屋里有x人,那么如果3个人一桌,多2个人.如果5个人一桌,多4个人.如果7个人一桌,多6个人.如果9个人一桌,多8个人.如果11个人一桌,正好.</h3><p>答： 由条件可知 ， (x + 1)  % 3 ==0,  (x + 1)  % 5 ==0,  (x + 1)  % 7 ==0,  (x + 1)  % 9 ==0,  所以，求 3，5，7，9的最小公倍数为 315，所以  x + 1 = 315m, (m 不定， m为整数)， 得到 x = 315m - 1,  </p>
<p>又  x % 11 == 0,  得到 等式  (315m - 1 ) % 11 == 0,  进一步得到 ( (308 + 7) m  - 1) % 11 == 0, </p>
<p>得到  （7m - 1）% 11 == 0 ，可以得到 m = 8 </p>
<p>最终得到  x = 315 * 8 - 1 = 2519 (答案不唯一，例如 x 还可以为 2519+315*11n， n 为 0 1 2 3 …..)</p>
<h3 id="百度：-还有一个是-一杯糖-一杯盐，一样多，然后一个k大的勺子-，先从-糖里面挖一勺到-盐里面-，混合之后，在从盐那个杯子里面挖一勺放回去，问-两个杯子里面杂质的占比哪个多？"><a href="#百度：-还有一个是-一杯糖-一杯盐，一样多，然后一个k大的勺子-，先从-糖里面挖一勺到-盐里面-，混合之后，在从盐那个杯子里面挖一勺放回去，问-两个杯子里面杂质的占比哪个多？" class="headerlink" title="百度：  还有一个是 一杯糖 一杯盐，一样多，然后一个k大的勺子 ，先从 糖里面挖一勺到 盐里面 ，混合之后，在从盐那个杯子里面挖一勺放回去，问 两个杯子里面杂质的占比哪个多？"></a>百度：  还有一个是 一杯糖 一杯盐，一样多，然后一个k大的勺子 ，先从 糖里面挖一勺到 盐里面 ，混合之后，在从盐那个杯子里面挖一勺放回去，问 两个杯子里面杂质的占比哪个多？</h3><p>一样多，假设杯子大小为 m，勺子大小为k， 可列公式：</p>
<p>盐那个杯子中的杂质占比：k/(m+k)，<br>混合之后，再挖一勺， 那这个勺子中的 盐的占比为：m/(m+k)，勺子中的盐有：m<em>k/(m + k)<br>糖那个杯子中的杂质占比：m</em>k/(m + k) /m = k/(m+k)</p>
<h3 id="百度：-共有100个球-甲乙轮流拿-一次最多能拿5个-拿到第100个球的人赢-如甲先拿-他要拿几个-才能赢"><a href="#百度：-共有100个球-甲乙轮流拿-一次最多能拿5个-拿到第100个球的人赢-如甲先拿-他要拿几个-才能赢" class="headerlink" title="百度：  共有100个球,甲乙轮流拿,一次最多能拿5个,拿到第100个球的人赢,如甲先拿,他要拿几个,才能赢"></a>百度：  共有100个球,甲乙轮流拿,一次最多能拿5个,拿到第100个球的人赢,如甲先拿,他要拿几个,才能赢</h3><p>答： 100÷6=16……4<br>拿法：<strong>甲先拿4个，以后的拿法为：乙拿x个，1≤x≤5，则甲拿6-x个</strong>。<br>乙拿一次甲拿一次算一轮的话，每轮都会减少6个球。<br>所以最后一轮会剩下6个，无论乙拿几个甲都会拿到最后1个 </p>
<h3 id="1-两根香，一根烧完1小时，如何测量15分钟-？"><a href="#1-两根香，一根烧完1小时，如何测量15分钟-？" class="headerlink" title="1  两根香，一根烧完1小时，如何测量15分钟 ？"></a>1  两根香，一根烧完1小时，如何测量15分钟 ？</h3><p> 先将一根香的一端点燃，另一根香的两端全部点燃。当第二根香全部烧完时，此时已经过了半个小时。再将第一根香的另一端也点燃，那么此时第一根香剩下部分烧完的时间就是 15 min。 </p>
<h3 id="2-场景题：互相关注表设计-场景题：需求：谁关注了我，我关注了谁，谁与我互相关注。表该如何设计，索引怎么建。查询语句怎么写-？"><a href="#2-场景题：互相关注表设计-场景题：需求：谁关注了我，我关注了谁，谁与我互相关注。表该如何设计，索引怎么建。查询语句怎么写-？" class="headerlink" title="2  场景题：互相关注表设计  场景题：需求：谁关注了我，我关注了谁，谁与我互相关注。表该如何设计，索引怎么建。查询语句怎么写 ？"></a>2  场景题：互相关注表设计  场景题：需求：谁关注了我，我关注了谁，谁与我互相关注。表该如何设计，索引怎么建。查询语句怎么写 ？</h3><p> 表可以有：id、粉丝id、被追随者id 三个字段，索引可以建两个，分别是粉丝 id 和被追随者 id。 </p>
<h3 id="3-（智力题）海盗分金币"><a href="#3-（智力题）海盗分金币" class="headerlink" title="3  （智力题）海盗分金币"></a>3  （智力题）海盗分金币</h3><p>有5个海盗，获得了100枚金币，于是他们要商量一个方法来分配金币。商议方式如下：<br> * (1) 由5个海盗轮流提出分配方案。<br> * (2) 如果超过半数海盗（包括提出者）同意该方案，则按照该方案分配。<br> * (3) 如果同意该方案的人数（包括提出者）小于等于半数，则提出者要被扔到海里喂鱼，剩下的海盗继续商议分配。<br> * (4) 海盗们都是绝对理性的，以自己尽可能多获得金币为目的。但是在收益相等的情况下，会倾向把提出者扔到海里。</p>
<p> 问：第一个海盗应该提出怎样的分配方案，才能保证自己既不被扔到海里，又能使自己利益最大化？ </p>
<p>答：博弈论题：假设 提出 方案的 海盗的顺序 为 ：  老大  —&gt; 老二  —&gt; 老三  —&gt; 老四  —&gt; 老五  </p>
<p>则  分配方案 ：    97     0    1   2  0   或者   97   0    1   0   2 </p>
<p>分析： 当剩下两个人的时候 ，  老四<strong>没有任何选择</strong>！无论他如何分配，哪怕把100枚金币都给老五，老五仍然可以反对，导致老四被扔到海里，金币全归老五所有。  老三心想：<strong>老四没有最优决策，所以无论我提出什么要求，老四都一定会同意，而老五一定不同意。</strong>  由于只要超过半数同意就可以执行分配，所以老三的最优策略如下： 100   0    0  ； 接下来，老二暗自寻思：<strong>如果没有我，老三能获得100枚金币，所以无论如何不会同意我</strong>。但我可以设法“笼络”老四和老五，形成 3 : 1 的局面。  因此，老二的最优策略如下： 98  0  1  1  ； 终于轮到老一了，老一心里琢磨：如果没有我，老二能获得98枚金币，<strong>我总不能分给他多于98枚，索性放弃他，只要剩下三人中笼络到两人，形成 3 : 2 的局面即可</strong>。 以老二的策略，老三得不到金币，所以老三最好“伺候”。<strong>我给老三1枚，老三一定同意</strong>。至于老四和老五，本来可以得到1枚，所以我必须比老二给的多，才能赢得支持。但我又没必要同时笼络他俩，要么给老四两枚金币，放弃老五，要么给老五两枚金币，放弃老四。 因此，老一的最优策略如下： 97 0  1  2  0  或者  97   0    1   0   2  </p>
<h3 id="4-ip-判断-：-有一个IP地址库，假设有几十万条ip，如何判断某个ip地址是否在这个库中-？"><a href="#4-ip-判断-：-有一个IP地址库，假设有几十万条ip，如何判断某个ip地址是否在这个库中-？" class="headerlink" title="4   ip 判断  ： 有一个IP地址库，假设有几十万条ip，如何判断某个ip地址是否在这个库中 ？"></a>4   ip 判断  ： 有一个IP地址库，假设有几十万条ip，如何判断某个ip地址是否在这个库中 ？</h3><p>方案1：布隆过滤器。<br> 布隆过滤器就是对多个 ip 通过多次 hash，将制定位置的标志设置为 1。在判断给定 ip 是否存在时，直接使用 ip 进行多次 hash，如果多次 hash 的索引所在值都为 1，那么存在。因此，布隆过滤器常用作垃圾短信、垃圾邮件的过滤机制（ps：布隆过滤器具有拒真、纳伪的特性）</p>
<p> 方案2：一致性 hash 加上多个表<br> 使用一致性 hash，将 ip 放到对应地址的表中。查找时进行一次 hash，找到所在的表，再在表中进行查找。 </p>
<h3 id="5-场景题：分布式生成数据-分布式多个机器生成id，如何保证不重复"><a href="#5-场景题：分布式生成数据-分布式多个机器生成id，如何保证不重复" class="headerlink" title="5  场景题：分布式生成数据   分布式多个机器生成id，如何保证不重复"></a>5  场景题：分布式生成数据   分布式多个机器生成id，如何保证不重复</h3><p>  使用 redis、数据库获取共同 id ， 或者 使用机器码进行 id 的生成 </p>
<h3 id="6-场景题：抛硬币问题-：-一个硬币，正面概率0-7，反面概率0-3，现在有一瓶水，怎么掷能让两个人公平的喝到水"><a href="#6-场景题：抛硬币问题-：-一个硬币，正面概率0-7，反面概率0-3，现在有一瓶水，怎么掷能让两个人公平的喝到水" class="headerlink" title="6 场景题：抛硬币问题  ： 一个硬币，正面概率0.7，反面概率0.3，现在有一瓶水，怎么掷能让两个人公平的喝到水"></a>6 场景题：抛硬币问题  ： 一个硬币，正面概率0.7，反面概率0.3，现在有一瓶水，怎么掷能让两个人公平的喝到水</h3><p>只要保证两个人喝水的概率一致即可。将硬币抛两次，那么出现情况的概率如下：</p>
<table>
<thead>
<tr>
<th>情况</th>
<th>正（第一次）</th>
<th>反（第一次）</th>
</tr>
</thead>
<tbody><tr>
<td>正（第二次）</td>
<td>0.49</td>
<td>0.21</td>
</tr>
<tr>
<td>反（第二次）</td>
<td>0.21</td>
<td>0.09</td>
</tr>
</tbody></table>
<p> 那么显然，只要让两个一正一反的情况出现时（ 0.21 == 0.21）让他们分别喝水，否则重抛即可。 </p>
<h3 id="7-概率题：扑克牌-：-54张扑克牌，平均分成3份，大小王在一份的概率"><a href="#7-概率题：扑克牌-：-54张扑克牌，平均分成3份，大小王在一份的概率" class="headerlink" title="7 概率题：扑克牌  ： 54张扑克牌，平均分成3份，大小王在一份的概率"></a>7 概率题：扑克牌  ： 54张扑克牌，平均分成3份，大小王在一份的概率</h3><p>$$<br>P = C_3^1 *C_{52}^{16} * C_{36}^{18} * C_{18}^{18} / C_{54}^{18} * C_{36}^{18} * C_{18}^{18}<br>$$</p>
<p> 分成3份 总的分法 M=(C54，18)<em>(C36，18)</em>(C18，18) 大小王在同一份N=(C3，1)<em>(C52，16)</em>(C36，18)*(C18，18) P=N /M=17/53 注：逗号后面为上标，前面为下标。 </p>
<h3 id="8-扫码登录是如何实现的？"><a href="#8-扫码登录是如何实现的？" class="headerlink" title="8    扫码登录是如何实现的？"></a>8    扫码登录是如何实现的？</h3><p>主要分成四个步骤：<br> * (1) 访问网站，网站服务器为当前会话生成一个全局唯一的 ID<br> * (2) 手机相关 APP 扫码，提示当前账号是否登录<br> * (3) app 将当前登录的账号信息和扫描得到的全局唯一的 ID 提交到服务器<br> * (4) 服务器将该 ID 和账号信息绑定，并通知网站登录该账号 </p>
<h3 id="9-思考题：赛马-64匹马，8个跑道，选跑最快的4匹马需要比赛多少次"><a href="#9-思考题：赛马-64匹马，8个跑道，选跑最快的4匹马需要比赛多少次" class="headerlink" title="9   思考题：赛马  : 64匹马，8个跑道，选跑最快的4匹马需要比赛多少次"></a>9   思考题：赛马  : 64匹马，8个跑道，选跑最快的4匹马需要比赛多少次</h3><p>(二分的思想 )首先将 64 匹马分成 8 组进行赛跑，取每组最快的 4 匹马。然后将这 32 匹分 4 组赛跑，再取 每组最快的 4 匹。再将剩下的 16 匹分两组跑，取最快的 8 匹再跑一次。因此答案是 8 + 4 + 2 + 1 = 15 次。</p>
<h3 id="10-思考题：抛硬币-两个人轮流抛硬币，先抛到正面的赢，问先抛的人赢的概率"><a href="#10-思考题：抛硬币-两个人轮流抛硬币，先抛到正面的赢，问先抛的人赢的概率" class="headerlink" title="10  思考题：抛硬币    两个人轮流抛硬币，先抛到正面的赢，问先抛的人赢的概率"></a>10  思考题：抛硬币    两个人轮流抛硬币，先抛到正面的赢，问先抛的人赢的概率</h3><p>题目没有额外说明，那么我们认为甲乙投硬币每次正反的概率都是 0.5。</p>
<p> 假设甲先投，那么甲赢的概率为 0.5（第一次投直接赢了） + 0.5 * (0.5 * 0.5)（第一次投没赢，乙投也没赢，第二次投赢了） + 0.5 * (0.5 * 0.5) * (0.5 * 0.5) + ···，我们可以看出这是一个等比数列求和，因此 P(甲) = a1 * (1 - q^n) /(1-q)，这边 q 显然为 0.5 * 0.5 = 0.25，带入的 P(甲) = 0.5 * (1 - 0.25^n) / 0.75。取极限 n -&gt; 正无穷，因此 P(甲) = 2/3 </p>
<h3 id="11-场景题：微博关注列表"><a href="#11-场景题：微博关注列表" class="headerlink" title="11  场景题：微博关注列表"></a>11  场景题：微博关注列表</h3><p> 一个很复杂的场景，大概是微博用户有个关注列表，以及知道各种大V的动态，设计数据结构啥的，到最后在用户这边显示一个更新微博列表 。</p>
<p>使用 加权有向图的数据结构， 使用  邻接矩阵 或者是 邻接链表来存储。</p>
<h3 id="12-场景题：游戏中的-debuff"><a href="#12-场景题：游戏中的-debuff" class="headerlink" title="12 场景题：游戏中的 debuff"></a>12 场景题：游戏中的 debuff</h3><p> 游戏里，一个玩家可能吃到很多种debuff，每个debuff有一定持续时间。给你一个定时器，怎么设计数据结构，来维护玩家的debuff状态？</p>
<p> 可能用一个队列加定时器？  </p>
<h3 id="13场景题：红黑树，B-树，跳表应用场景"><a href="#13场景题：红黑树，B-树，跳表应用场景" class="headerlink" title="13场景题：红黑树，B+树，跳表应用场景"></a>13场景题：红黑树，B+树，跳表应用场景</h3><p> 红黑树 - HashMap；<br>B+ 树 - Mysql inoodb 索引结构；<br>跳表 - redis 的数据结构 zset </p>
<h3 id="14-公交车超载"><a href="#14-公交车超载" class="headerlink" title="14   公交车超载"></a>14   公交车超载</h3><p> 一辆公交车，有 m 站，最多坐 n 人，输入一路上票的信息（即上车下车站），输出会不会超载。 </p>
<p>求和 就行</p>
<h3 id="15-场景题：台球入洞"><a href="#15-场景题：台球入洞" class="headerlink" title="15  场景题：台球入洞"></a>15  场景题：台球入洞</h3><p> 坐标系中有一个球桌，四个角坐标：(0,0), (0,4), (2,4), (2,0)，一颗球在 (1,1)，请问从哪些角度可以射入洞内（可无限次碰撞）？ </p>
<p><strong>思路</strong>：</p>
<p>画画图，大概 6 种？ </p>
<h3 id="16-手写jdk中的优先级队列-PriorityQueue（最小堆）"><a href="#16-手写jdk中的优先级队列-PriorityQueue（最小堆）" class="headerlink" title="16  手写jdk中的优先级队列 PriorityQueue（最小堆）"></a>16  手写jdk中的优先级队列 PriorityQueue（最小堆）</h3>]]></content>
      <tags>
        <tag>智力题</tag>
      </tags>
  </entry>
  <entry>
    <title>九大排序</title>
    <url>/2020/06/24/%E4%B9%9D%E5%A4%A7%E6%8E%92%E5%BA%8F/</url>
    <content><![CDATA[<h2 id="九大基本排序原理，比较，复杂度，是否稳定，Java实现"><a href="#九大基本排序原理，比较，复杂度，是否稳定，Java实现" class="headerlink" title="九大基本排序原理，比较，复杂度，是否稳定，Java实现"></a>九大基本排序原理，比较，复杂度，是否稳定，Java实现</h2><p>[TOC]</p>
<p><img src="/2020/06/24/%E4%B9%9D%E5%A4%A7%E6%8E%92%E5%BA%8F/image-1.png" alt="image-20200624181122715"></p>
<p><img src="/2020/06/24/%E4%B9%9D%E5%A4%A7%E6%8E%92%E5%BA%8F/image-2.png" alt="image-20200624182504146"></p>
<a id="more"></a>

<p>以下介绍按照重要程度来的。不会介绍具体的实现过程，重点在于记录每种排序的特点，复杂度，稳定性，区别和使用场景。具体的实现过程使用Java代码描述。</p>
<p>稳定性： 判断一个排序算法是否稳定，即 排序前后 数组中两个相同元素的相对位置不发生改变，就是稳定的排序算法。</p>
<p>例 ：[4,3’,5,3’’,6,8]  ====&gt;  [3’,3’’,4,5,6,8] 即  两个相同的3 排序前后 它们的前后位置 是一致的，即为稳定。</p>
<h2 id="1-快速排序"><a href="#1-快速排序" class="headerlink" title="1. 快速排序"></a>1. 快速排序</h2><p>算法描述:任取待排序中的某一元素作为基准值,按照该基准值,将待排序集合分割成两个子序列,左边的序列比基准值小,右边的比基准值大.然后对左右子序列重复上述步骤.直到排完为止。</p>
<p> <strong>稳定性：不稳定。</strong> 因为在最后一次 arr[low] 与 arr[i]  交换时，如果相同的话会导致顺序变化 </p>
<p> <strong>最坏情况复杂度：</strong>整个数组是逆序的。[8,7,6,5,4,3,2,1]， 每次划分只得到一个比上一次划分少一个记录的子序列 ， 此时它的复杂度为 O(n^2)。计算过程：<br>$$<br>T = n -1 + n -2 + n-3 …. + 1 = n(n - 1) / 2 = O(N^2)<br>$$<br><strong>平均情况复杂度：</strong><br>$$<br>T(n) = n + 2T(n / 2) = n + 2( n/ 2 + 2T(n/4)) = n +  2( n/ 2 + 2(n/4 + 2T(n/8)) = …<br>$$</p>
<p>$$<br>T(n) = n + 2T(n/2) = 2n + 4(n/4) = 3n + 8T(n/8) =…= kn + 2^kT(n/2^k)<br>$$</p>
<p>$$<br>又当 n / 2^k = 1 时， k = logN ,将 k = logN 带入上式：<br>$$</p>
<p>$$<br>T(N) = N<em>logN + N</em>T(1) = O(NlogN)<br>$$<br>使用场景：是最快的通用排序算法，大多数使用情况下，是最佳选择。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">    * <span class="doctag">@author</span>: xiongcong</span></span><br><span class="line"><span class="comment">    * <span class="doctag">@Date</span>: 2020/3/20 14:59</span></span><br><span class="line"><span class="comment">    * <span class="doctag">@Description</span>: 递归实现快排</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">quickSort</span><span class="params">(<span class="keyword">int</span>[] arr, <span class="keyword">int</span> low, <span class="keyword">int</span> high)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">int</span> i, j, temp, t;</span><br><span class="line">       <span class="keyword">if</span> (low &gt; high) &#123;</span><br><span class="line">           <span class="keyword">return</span>;</span><br><span class="line">       &#125;</span><br><span class="line">       i = low;</span><br><span class="line">       j = high;</span><br><span class="line">       <span class="comment">//temp就是基准位</span></span><br><span class="line">       temp = arr[low];</span><br><span class="line"></span><br><span class="line">       <span class="keyword">while</span> (i &lt; j) &#123;</span><br><span class="line">           <span class="comment">//先看右边，依次往左递减 找小于基准值的 或者 i == j  （这两个while 顺序不能反）</span></span><br><span class="line">           <span class="keyword">while</span> (temp &lt;= arr[j] &amp;&amp; i &lt; j) &#123;</span><br><span class="line">               j--;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="comment">//再看左边，依次往右递增 找大于基准值的 或者 i == j</span></span><br><span class="line">           <span class="keyword">while</span> (temp &gt;= arr[i] &amp;&amp; i &lt; j) &#123;</span><br><span class="line">               i++;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="comment">//如果满足条件则交换</span></span><br><span class="line">           <span class="keyword">if</span> (i &lt; j) &#123;</span><br><span class="line">               t = arr[j];</span><br><span class="line">               arr[j] = arr[i];</span><br><span class="line">               arr[i] = t;</span><br><span class="line">           &#125;</span><br><span class="line"></span><br><span class="line">       &#125;</span><br><span class="line">       <span class="comment">//最后将基准为与i和j相等位置的数字交换</span></span><br><span class="line">       arr[low] = arr[i];</span><br><span class="line">       arr[i] = temp;</span><br><span class="line">       <span class="comment">//递归调用左半数组</span></span><br><span class="line">       quickSort(arr, low,  i - <span class="number">1</span>);</span><br><span class="line">       <span class="comment">//递归调用右半数组</span></span><br><span class="line">       quickSort(arr,  i + <span class="number">1</span>, high);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-堆排序"><a href="#2-堆排序" class="headerlink" title="2. 堆排序"></a>2. 堆排序</h2><p>堆排序的基本思想是：将待排序序列构造成一个大顶堆，此时，整个序列的最大值就是堆顶的根节点。将其与末尾元素进行交换， 此时末尾就为最大值。然后将剩余n-1个元素重新构造成一个堆， 这样会得到n个元素的次小值。如此反复执行，便能得到一个有序序列了。</p>
<p>堆 这里指的是 二叉堆，有大顶堆和小顶堆之分，其具体含义是，若是大顶堆，则 根节点的 值 大于等于它的 左右子树的值， 这个性质具有递归性。每个结点的左子树和右子树都是一个二叉堆（都是最大堆或最小堆）。<br>与 二叉树不同，二叉堆 是通过数组的方式实现的，所以我们可以利用它的性质来对数组进行排序。<br>具体过程是：<br>    1首先将原数组构造成为一个二叉堆，这里我们默认构造大顶堆，那么它堆顶元素就是最大的。<br>    2然后将  堆顶 ，即数组中的第一个位置的元素和 数组末尾元素 交换。<br>    3然后对剩余的  0 ~ n -1 的部分进行调整，使之重新满足 大顶堆的性质。<br>    4然后 重复 2 3 两步，直到整个数组有序。</p>
<p>最开始构造二叉堆时，可以从最后一个非叶子节点 的位置开始，反向遍历构造 （ (n / 2 - 1) <del>~</del> 0）</p>
<p><strong>堆排序时间复杂度：O(NlogN)</strong><br>    初始化建堆过程时间：O(n)：（从最后一个非叶子节点开始）， k 为 堆的高度 k = logN<br>    总的时间计算为：s = (2^( i - 1 )) * ( k - i )；其中 i 表示第几层，2^(i - 1) 表示该层上有多少个元素，(k - i) 表示子树上要比较的次数.<br>$$<br>S = 2^(k-2) * 1 + 2^(k-3)<em>2…..+2^(k-2)+2^(0)</em>(k-1)  ===&gt; S = 2^k -k -1 代入k : S = N - logN -1<br>$$</p>
<pre><code>更改堆元素后重建堆时间：O(nlogn):
首先，将一个元素调整到二叉堆中合适的位置，其时间复杂度为 这个 二叉堆的高度 即 logN
然后 我们需要处理整个 数组 中的N 个元素。
合起来 他的时间复杂度为 O(NlogN)</code></pre><p><strong>堆排序是不稳定的。</strong>因为在堆性质维护的时候，可能导致相同的数据位置发生变换。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//堆排序</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] arr = &#123;<span class="number">25</span>,<span class="number">84</span>,<span class="number">21</span>,<span class="number">47</span>,<span class="number">15</span>,<span class="number">27</span>,<span class="number">68</span>,<span class="number">35</span>,<span class="number">20</span>&#125;;</span><br><span class="line">        <span class="keyword">new</span> HeapSort().heapSort(arr);</span><br><span class="line">        System.out.println(Arrays.toString(arr));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//1 将无序序列构建成一个堆，根据升序降序需求选择大顶堆或小顶堆;</span></span><br><span class="line">    <span class="comment">//1.1 从最后一个非叶子结点开始 第一个非叶子结点下标 x = len / 2 - 1</span></span><br><span class="line">    <span class="comment">// 怎么算的呢？</span></span><br><span class="line">    <span class="comment">// len 为奇数 则右叶子节点存在，2*x + 2 = len - 1 ----&gt; x = (len - 1)/2 -1</span></span><br><span class="line">    <span class="comment">// len 为偶数 则右叶子节点不存在， 2*x + 1 = len - 1 ----&gt; x = len / 2 - 1</span></span><br><span class="line">    <span class="comment">//又因为 当 len 为奇数时， len/2 的 取整结果 即为 (len - 1)/ 2</span></span><br><span class="line">    <span class="comment">// 综合两种情况， 可以得到 x = len / 2 - 1</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span>[] heapSort(<span class="keyword">int</span>[] nums) &#123;</span><br><span class="line">        <span class="keyword">if</span> (nums == <span class="keyword">null</span> || nums.length &lt; <span class="number">2</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> nums;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 堆排序</span></span><br><span class="line">        <span class="keyword">int</span> n = nums.length;</span><br><span class="line">        <span class="comment">// 建堆， 大顶堆</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = n / <span class="number">2</span> - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">            adjust(nums, i, n);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 排序</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = n - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">            swap(nums, <span class="number">0</span>, i);</span><br><span class="line">            <span class="comment">// 调整</span></span><br><span class="line">            adjust(nums, <span class="number">0</span>, i);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> nums;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 前提是  arr 在调整之前基本是一个 大顶堆，即除了 当前元素 ，其他 元素都在合适的位置上</span></span><br><span class="line">    <span class="comment">// 调整大顶堆 将 arr 中 从 i 到 curLen 的所有元素调整为 一个大顶堆</span></span><br><span class="line">    <span class="comment">// 该函数只是 将 位置 i 的元素放到了 大顶堆的 合适的位置，</span></span><br><span class="line">    <span class="comment">// arr  当前节点下标  当前堆的长度。 复杂度  logn</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">adjust</span><span class="params">(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> cur, <span class="keyword">int</span> curLen)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 从 左子节点开始</span></span><br><span class="line">        <span class="keyword">while</span> (<span class="number">2</span> * cur + <span class="number">1</span> &lt; curLen) &#123;</span><br><span class="line">            <span class="keyword">int</span> i = <span class="number">2</span> * cur + <span class="number">1</span>;</span><br><span class="line">            <span class="comment">// 若有右子节点 则判断是否大于 左</span></span><br><span class="line">            <span class="keyword">if</span> (i + <span class="number">1</span> &lt; curLen &amp;&amp; nums[i] &lt; nums[i + <span class="number">1</span>]) &#123;</span><br><span class="line">                i++;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (nums[i] &gt; nums[cur]) &#123;</span><br><span class="line">                swap(nums, i, cur);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            cur = i;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">swap</span><span class="params">(<span class="keyword">int</span>[] arr, <span class="keyword">int</span> i, <span class="keyword">int</span> j)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> temp = arr[i];</span><br><span class="line">        arr[i] = arr[j];</span><br><span class="line">        arr[j] = temp;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>



<h2 id="3-归并排序"><a href="#3-归并排序" class="headerlink" title="3. 归并排序"></a>3. 归并排序</h2><p>归并排序（MERGE-SORT）是建立在归并操作上的一种有效的排序算法,</p>
<ul>
<li>该算法是采用分治法（Divide and Conquer）的一个非常典型的应用。</li>
<li>将已有序的子序列合并，得到完全有序的序列；即先使每个子序列有序，再使子序列段间有序。</li>
<li>若将两个有序表合并成一个有序表，称为二路归并。</li>
</ul>
<p><strong>时间复杂度 O(N*logN) ；空间复杂度 O(N)</strong></p>
<p><strong>稳定性：稳定。</strong></p>
<p> 归并排序并不是一种原地排序，因为需要额外申请空间来充当临时容器。</p>
<p>可以利用该排序 求解逆序对问题。<a href="https://leetcode-cn.com/problems/shu-zu-zhong-de-ni-xu-dui-lcof/" target="_blank" rel="noopener">剑指 Offer 51. 数组中的逆序对</a></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span>[] temp; <span class="comment">// 辅助数组 放在 merge 外面</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span>[] mergeSort(<span class="keyword">int</span>[] arr) &#123;</span><br><span class="line">    <span class="keyword">if</span> (arr == <span class="keyword">null</span> || arr.length &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> arr;</span><br><span class="line">    &#125;</span><br><span class="line">    temp = <span class="keyword">new</span> <span class="keyword">int</span>[arr.length];</span><br><span class="line">    mergeSort(arr, <span class="number">0</span>, arr.length - <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//归并排序</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">mergeSort</span><span class="params">(<span class="keyword">int</span>[] arr, <span class="keyword">int</span> left, <span class="keyword">int</span> right)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (right &lt;= left) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> mid = left + (right - left) / <span class="number">2</span>;</span><br><span class="line">    <span class="comment">//拆分</span></span><br><span class="line">    mergeSort(arr, left, mid);</span><br><span class="line">    <span class="comment">//拆分</span></span><br><span class="line">    mergeSort(arr, mid + <span class="number">1</span>, right);</span><br><span class="line">    <span class="comment">//合并</span></span><br><span class="line">    merge(arr, left, mid, right);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//合并</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">merge</span><span class="params">(<span class="keyword">int</span>[] arr, <span class="keyword">int</span> left, <span class="keyword">int</span> mid, <span class="keyword">int</span> right)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i = left; <span class="comment">// 左的起点</span></span><br><span class="line">    <span class="keyword">int</span> j = mid + <span class="number">1</span>; <span class="comment">// 右的起点</span></span><br><span class="line">    <span class="keyword">int</span> k = left; <span class="comment">// 临时数组的 起点 ！！！！ 不要设置为 0</span></span><br><span class="line">    <span class="comment">//合并</span></span><br><span class="line">    <span class="keyword">while</span> (i &lt;= mid &amp;&amp; j &lt;= right) &#123;</span><br><span class="line">        <span class="keyword">if</span> (arr[i] &lt;= arr[j]) &#123;  <span class="comment">//  &lt;= 则 为稳定排序  如果写 严格小于 &lt; 则不稳定</span></span><br><span class="line">            temp[k] = arr[i];</span><br><span class="line">            i++;</span><br><span class="line">            k++;</span><br><span class="line">            <span class="comment">//arr[i] &lt;= arr[j]  证明从 mid+1 到 j 个数  不包括 j 位置本身 小于 arr[i]</span></span><br><span class="line">           <span class="comment">// count += j - (mid + 1); // 这时计算 逆序度</span></span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            temp[k] = arr[j];</span><br><span class="line">            j++;</span><br><span class="line">            k++;</span><br><span class="line">            <span class="comment">// 或者</span></span><br><span class="line">            <span class="comment">// count += mid - i + 1;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 当 左边还有剩余</span></span><br><span class="line">    <span class="keyword">while</span> (i &lt;= mid) &#123;</span><br><span class="line">        temp[k++] = arr[i++];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 当 右边还有剩余</span></span><br><span class="line">    <span class="keyword">while</span> (j &lt;= right) &#123;</span><br><span class="line">        temp[k++] = arr[j++];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//从temp 到 arr</span></span><br><span class="line">    <span class="keyword">while</span> (left &lt;= right) &#123;</span><br><span class="line">        arr[left] = temp[left];</span><br><span class="line">        left++;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="4-冒泡排序"><a href="#4-冒泡排序" class="headerlink" title="4. 冒泡排序"></a>4. 冒泡排序</h2><p>算法描述:比较两个相邻的元素,如果升序的话,前面的比后面的大,就交换,这样一轮下来,就会找到这组数据中最大的元素,然后抛开这个元素继续重复上述步骤.直到排完为止.  </p>
<p>时间复杂度：O(n^2)</p>
<p>稳定性：稳定（如果判断相等时，不进行元素的交换）</p>
<p>可以使用一个标志位来进行简单的优化。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 冒泡排序 时间复杂度O(n^2)</span></span><br><span class="line"><span class="comment"> * </span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> arr</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span>[] bubbleSort(<span class="keyword">int</span>[] arr) &#123;</span><br><span class="line">       <span class="keyword">if</span> (arr == <span class="keyword">null</span> || arr.length &lt; <span class="number">2</span>)&#123;</span><br><span class="line">           <span class="keyword">return</span> arr;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">int</span> n = arr.length;</span><br><span class="line">       <span class="keyword">int</span> flag = <span class="number">0</span>;</span><br><span class="line">       <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)&#123;</span><br><span class="line">           flag = <span class="number">0</span>;</span><br><span class="line">           <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; n - i - <span class="number">1</span>; j++)&#123;</span><br><span class="line">               <span class="keyword">if</span> (arr[j] &gt; arr[j + <span class="number">1</span>])&#123;</span><br><span class="line">                   flag = <span class="number">1</span>;</span><br><span class="line">                   swap(arr, j, j + <span class="number">1</span>);</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="keyword">if</span> (flag == <span class="number">0</span>)&#123; <span class="comment">//证明 这一轮中，没有元素进行过交换，已经是 sorted 了</span></span><br><span class="line">               <span class="keyword">return</span> arr;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">return</span> arr;    </span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">swap</span><span class="params">(<span class="keyword">int</span>[] arr, <span class="keyword">int</span> i, <span class="keyword">int</span> j)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">int</span> temp = arr[i];</span><br><span class="line">       arr[i] = arr[j];</span><br><span class="line">       arr[j] = temp;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<h2 id="5-选择排序"><a href="#5-选择排序" class="headerlink" title="5. 选择排序"></a>5. 选择排序</h2><p>选择排序: 先找出最小的元素放到新数组中， 然后更新原数组，即去掉原数组中的这个最小元素。</p>
<p>时间复杂度 O(n^2)</p>
<p>空间复杂度 O(n)</p>
<p>稳定性：不稳定。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">int</span>[] selectSort(<span class="keyword">int</span>[] arr)&#123;</span><br><span class="line">    <span class="keyword">if</span> (arr == <span class="keyword">null</span> || arr.length &lt; <span class="number">2</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> arr;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> n = arr.length;</span><br><span class="line">    <span class="keyword">int</span>[] newArr = <span class="keyword">new</span> <span class="keyword">int</span>[n];</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)&#123;</span><br><span class="line">        <span class="keyword">int</span> smallest = findSmallest(arr, n - i);</span><br><span class="line">        newArr[i] = smallest;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> newArr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">findSmallest</span><span class="params">(<span class="keyword">int</span>[] arr, <span class="keyword">int</span> end)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> ans = arr[<span class="number">0</span>];</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; end; i++)&#123;</span><br><span class="line">        <span class="keyword">if</span> (ans &gt; arr[i])&#123;</span><br><span class="line">            ans = arr[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//把这个最小的元素交换到数组最后</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; end; i++)&#123;</span><br><span class="line">        <span class="keyword">if</span> (ans == arr[i])&#123;</span><br><span class="line">            <span class="keyword">int</span> temp = arr[i];</span><br><span class="line">            arr[i] = arr[end - <span class="number">1</span>];</span><br><span class="line">            arr[end - <span class="number">1</span>] = temp;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ans;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="6-插入排序"><a href="#6-插入排序" class="headerlink" title="6. 插入排序"></a>6. 插入排序</h2><p>直接插入排序的基本操作就是将一个数据插入到已经排好序的有序数据中，从而得到一个新的、个数加一的有序数据， 算法适用于少量数据的排序。</p>
<p>时间复杂度为O(n^2)。</p>
<p>稳定性：稳定。</p>
<p>插入排序在原本升序排升序,原本逆序排逆序,元素集合接近有序时,直接插入排序效率较高.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">int</span>[] insertSort(<span class="keyword">int</span>[] arr)&#123;</span><br><span class="line">    <span class="keyword">if</span> (arr == <span class="keyword">null</span> || arr.length &lt; <span class="number">2</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> arr;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> n = arr.length;</span><br><span class="line">    <span class="keyword">int</span> temp;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; n; i++)&#123;</span><br><span class="line">        <span class="comment">// 保留当前值</span></span><br><span class="line">        temp = arr[i];</span><br><span class="line">        <span class="comment">// 与当前值 之前的元素一一比较</span></span><br><span class="line">        <span class="keyword">int</span> j = i - <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">while</span>(j &gt;= <span class="number">0</span> &amp;&amp; arr[j] &gt; temp)&#123;</span><br><span class="line">            <span class="comment">// 往后退一个位置，让当前数据与它前一位的前一位进行比较</span></span><br><span class="line">            arr[j + <span class="number">1</span>] = arr[j];</span><br><span class="line">        	j--;</span><br><span class="line">    	&#125;</span><br><span class="line">        <span class="comment">// 找到了要插入的位置</span></span><br><span class="line">        arr[j + <span class="number">1</span>] = temp;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="7-希尔排序"><a href="#7-希尔排序" class="headerlink" title="7. 希尔排序"></a>7. 希尔排序</h2><p>希尔排序(Shell’s Sort)是插入排序的一种又称“缩小增量排序”（Diminishing Increment Sort），是直接插入排序算法的一种更高效的改进版本。该方法因D.L.Shell于1959年提出而得名。</p>
<p>时间复杂度：</p>
<p>稳定性： 不稳定。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span>[] shellSort(<span class="keyword">int</span>[] arrays) &#123;</span><br><span class="line">		<span class="comment">// 增量每次都/2</span></span><br><span class="line">		<span class="keyword">for</span> (<span class="keyword">int</span> step = arrays.length / <span class="number">2</span>; step &gt; <span class="number">0</span>; step /= <span class="number">2</span>) &#123;</span><br><span class="line">			<span class="comment">// 从增量那组开始进行插入排序，直至完毕</span></span><br><span class="line">			<span class="comment">/**********************每组都进行插入排序****************/</span></span><br><span class="line">			<span class="keyword">for</span> (<span class="keyword">int</span> i = step; i &lt; arrays.length; i++) &#123;</span><br><span class="line"></span><br><span class="line">				<span class="keyword">int</span> j = i;</span><br><span class="line">				<span class="keyword">int</span> temp = arrays[j];</span><br><span class="line">				<span class="comment">// j - step 就是代表与它同组隔壁的元素</span></span><br><span class="line">				<span class="keyword">while</span> (j - step &gt;= <span class="number">0</span> &amp;&amp; arrays[j - step] &gt; temp) &#123;</span><br><span class="line">					arrays[j] = arrays[j - step];</span><br><span class="line">					j = j - step;</span><br><span class="line">				&#125;</span><br><span class="line">				arrays[j] = temp;</span><br><span class="line">			&#125;</span><br><span class="line">			<span class="comment">/*******************************************/</span></span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> arrays;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<h2 id="8-计数排序-（桶排序）"><a href="#8-计数排序-（桶排序）" class="headerlink" title="8. 计数排序 （桶排序）"></a>8. 计数排序 （桶排序）</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">    *  <span class="doctag">@author</span>: xiongcong</span></span><br><span class="line"><span class="comment">    *  <span class="doctag">@Date</span>: 2019/9/19 14:49</span></span><br><span class="line"><span class="comment">    *  <span class="doctag">@Description</span>: 桶排序 简易版</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span>[] bucketSort(<span class="keyword">int</span>[] arr, <span class="keyword">int</span> max)&#123;</span><br><span class="line">       <span class="keyword">if</span> (arr == <span class="keyword">null</span> || arr.length &lt; <span class="number">2</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> arr;</span><br><span class="line">       &#125;</span><br><span class="line">   	<span class="keyword">int</span>[] bucket = <span class="keyword">new</span> <span class="keyword">int</span>[max + <span class="number">1</span>];<span class="comment">//最大值加1 因为数组下标是从0 开始的</span></span><br><span class="line">	<span class="comment">//统计每个元素出现的个数</span></span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt;arr.length ; i++) &#123;</span><br><span class="line">		bucket[arr[i]]++;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">int</span> j=<span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; max + <span class="number">1</span>; i++) &#123;</span><br><span class="line">		<span class="keyword">while</span>(bucket[i]&gt;<span class="number">0</span>)&#123;</span><br><span class="line">			arr[j++]=i;</span><br><span class="line">			bucket[i]--;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">       <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="9-基数排序"><a href="#9-基数排序" class="headerlink" title="9. 基数排序"></a>9. 基数排序</h2>]]></content>
  </entry>
  <entry>
    <title>TCP连接的建立和关闭</title>
    <url>/2020/05/04/TCP%E8%BF%9E%E6%8E%A5%E7%9A%84%E5%BB%BA%E7%AB%8B%E5%92%8C%E5%85%B3%E9%97%AD/</url>
    <content><![CDATA[<h3 id="TCP头部"><a href="#TCP头部" class="headerlink" title="TCP头部"></a>TCP头部</h3><p>每个T C P 头部包含了源和目的端口号。这两个值与I P 头部中的源和目的I P 地址一起,唯一地标识了每个连接。在T C P 术语中, 一个 I P 地址和一个端口的组合有时被称为一个端点( endpoint ) 或套接字( socket)。</p>
<a id="more"></a>

<p><img src="/2020/05/04/TCP%E8%BF%9E%E6%8E%A5%E7%9A%84%E5%BB%BA%E7%AB%8B%E5%92%8C%E5%85%B3%E9%97%AD/image-20200504173941089.png" alt="image-20200504173941089"></p>
<h3 id="TCP连接的建立和终止"><a href="#TCP连接的建立和终止" class="headerlink" title="TCP连接的建立和终止"></a>TCP连接的建立和终止</h3><p>一个TCP 连接由一个4 元组构成, 它们分别是两个 I P 地址和两个端日号。更准确地说, 一个T C P 连接是由一对端点或套接字构成, 其中通信的每一端都由一对( I P 地址, 端口号) 所唯一标识。</p>
<p>一个TCP 连接通常分为3 个阶段：启动、数据传输( 也称作“连接已建立”) 和退出( 关闭) 。</p>
<p><img src="/2020/05/04/TCP%E8%BF%9E%E6%8E%A5%E7%9A%84%E5%BB%BA%E7%AB%8B%E5%92%8C%E5%85%B3%E9%97%AD/image-20200504172429077.png" alt="image-20200504172429077"></p>
<p>上图显示了一个典型的 T C P 连接的建立与关闭过程( 不包括任何数据传输) ，一个普通T C P 连接的建立与终止。通常, 由客户端负责发起一个三次握手过程。在该过程中, 客户端与服务器利用S Y N 报文段交换彼此的初始序列号( 包括客户端的初始序列号和服务器的初始序列号) 。在通信双方都发送了一个F I N 数据包并收到来自对方的相应的确认数据包后, 该连接终止。</p>
<h4 id="TCP-连接的建立"><a href="#TCP-连接的建立" class="headerlink" title="TCP 连接的建立"></a>TCP 连接的建立</h4><p>为了建立一个T C P 连接,需要完成以下步骤:</p>
<ol>
<li>主动开启者( 通常称为客户端) 发送一个S Y N 报文段( 即一个在T C P 头部的S Y N  位字段置位的T C P / I P 数据包) , 并指明自己想要连接的端口号和它的客户端初始序列号( 记为I S N ( c ) ) 。通常, 客户端发送的这个S Y N 报文段称作段1。</li>
<li>服务器也发送自已的S Y N 报文段作为响应, 并包含了它的初始序列号( 记作I S N ( s ) ) 。该段称作段2。此外, 为了确认客户端的S Y N , 服务器将其包含的I S N ( C ) 数值加1后作为返回的 ack 数值。因此, 每发送一个S Y N , 序列号就会自动加1。 这样如果出现丢失的情况, 该S Y N 段将会重传。</li>
<li>为了确认服务器的S Y N , 客户端将I S N ( S ) 的数值加1 后作为返回的 ack 数值。这称作段3。</li>
</ol>
<p>通过发送上述3 个报文段就能够完成一个T C P 连接的建立。它们也常称作三次握手。三次握手的目的不仅在于让通信双方了解一个连接正在建立, 还在于利用数据包的选项来承载特殊的信息, 交换初始序列号( I n i t i a l S e q u e n c e N u m b e r , I S N ) 。发送首个S Y N 的一方被认为是主动地打开一个连接。 它通常是一个客户端。连接的另一方会接收这个S Y N , 并发送下一个S Y N , 因此它是被动地打开一个连接。通常, 这一方称为服务器。 </p>
<h4 id="TCP连接的关闭"><a href="#TCP连接的关闭" class="headerlink" title="TCP连接的关闭"></a>TCP连接的关闭</h4><p>连接的任何一方都能够发起一个关闭操作。此外, 该过程还支持双方同时关闭连接的操作, 但这种情况非常少见。在传统的情况下, 负责发起关闭连接的通常是客户端( 如图1 3 - 1 所示) 。然而, 一些服务器( 例如w e b 服务器) 在对请求做出响应之后也会发起一个关闭操作。通常一个关闭操作是由应用程序提出关闭连接的请求而引发的( 例如使用系统调用 close() ) 。T C P 协议规定通过发送一个F I N 段( 即F I N 位字段置位的T C P 报文段) 来发起关闭操作。</p>
<p>只有当连接双方都完成关闭操作后, 才构成一个完整关闭：</p>
<ol>
<li>客户端进程发出连接释放报文，并且停止发送数据。释放数据报文首部，FIN=1，其序列号为seq=u（等于前面已经传送过来的数据的最后一个字节的序号加1），此时，客户端进入FIN-WAIT-1（终止等待1）状态。 TCP规定，FIN报文段即使不携带数据，也要消耗一个序号。</li>
<li>服务器收到连接释放报文，发出确认报文，ACK=1，ack=u+1，并且带上自己的序列号seq=v，此时，服务端就进入了CLOSE-WAIT（关闭等待）状态。TCP服务器通知高层的应用进程，客户端向服务器的方向就释放了，这时候处于半关闭状态，即客户端已经没有数据要发送了，但是服务器若发送数据，客户端依然要接受。这个状态还要持续一段时间，也就是整个CLOSE-WAIT状态持续的时间。</li>
<li>客户端收到服务器的确认请求后，此时，客户端就进入FIN-WAIT-2（终止等待2）状态，等待服务器发送连接释放报文（在这之前还需要接受服务器发送的最后的数据）。</li>
<li>服务器将最后的数据发送完毕后，就向客户端发送连接释放报文，FIN=1，ack=u+1，由于在半关闭状态，服务器很可能又发送了一些数据，假定此时的序列号为seq=w，此时，服务器就进入了LAST-ACK（最后确认）状态，等待客户端的确认。</li>
<li>客户端收到服务器的连接释放报文后，必须发出确认，ACK=1，ack=w+1，而自己的序列号是seq=u+1，此时，客户端就进入了TIME-WAIT（时间等待）状态。注意此时TCP连接还没有释放，必须经过2∗∗MSL（最长报文段寿命）的时间后，当客户端撤销相应的TCB后，才进入CLOSED状态。</li>
<li>服务器只要收到了客户端发出的确认，立即进入CLOSED状态。同样，撤销TCB后，就结束了这次的TCP连接。可以看到，服务器结束TCP连接的时间要比客户端早一些。（此时客户端还要等待2∗∗MSL）。</li>
</ol>
<p>综上所述, 建立一个T C P 连接需要3 个报文段, 而关闭一个T C P 连接需要4 个报文段。</p>
<h3 id="常见面试题"><a href="#常见面试题" class="headerlink" title="常见面试题"></a>常见面试题</h3><p>【问题1】为什么连接的时候是三次握手，关闭的时候却是四次握手？</p>
<p>答：因为当Server端收到Client端的SYN连接请求报文后，可以直接发送SYN+ACK报文。其中ACK报文是用来应答的，SYN报文是用来同步的。但是关闭连接时，当Server端收到FIN报文时，很可能并不会立即关闭SOCKET，所以只能先回复一个ACK报文，告诉Client端，”你发的FIN报文我收到了”。只有等到我Server端所有的报文都发送完了，我才能发送FIN报文，因此不能一起发送。故需要四步握手。</p>
<p>【问题2】为什么TIME_WAIT状态需要经过2MSL(最大报文段生存时间)才能返回到CLOSE状态？</p>
<p>答：虽然按道理，四个报文都发送完毕，我们可以直接进入CLOSE状态了，但是我们必须假象网络是不可靠的，有可以最后一个ACK丢失。所以TIME_WAIT状态就是用来重发可能丢失的ACK报文。在Client发送出最后的ACK回复，但该ACK可能丢失。Server如果没有收到ACK，将不断重复发送FIN片段。所以Client不能立即关闭，它必须确认Server接收到了该ACK。Client会在发送出ACK之后进入到TIME_WAIT状态。Client会设置一个计时器，等待2MSL的时间。如果在该时间内再次收到FIN，那么Client会重发ACK并再次等待2MSL。所谓的2MSL是两倍的MSL(Maximum Segment Lifetime)。MSL指一个片段在网络中最大的存活时间，2MSL就是一个发送和一个回复所需的最大时间。如果直到2MSL，Client都没有再次收到FIN，那么Client推断ACK已经被成功接收，则结束TCP连接。</p>
<p>【问题3】为什么不能用两次握手进行连接？</p>
<p>答：3次握手完成两个重要的功能，既要双方做好发送数据的准备工作(双方都知道彼此已准备好)，也要允许双方就初始序列号进行协商，这个序列号在握手过程中被发送和确认。</p>
<p>​        现在把三次握手改成仅需要两次握手，死锁是可能发生的。作为例子，考虑计算机S和C之间的通信，假定C给S发送一个连接请求分组，S收到了这个分组，并发 送了确认应答分组。按照两次握手的协定，S认为连接已经成功地建立了，可以开始发送数据分组。可是，C在S的应答分组在传输中被丢失的情况下，将不知道S 是否已准备好，不知道S建立什么样的序列号，C甚至怀疑S是否收到自己的连接请求分组。在这种情况下，C认为连接还未建立成功，将忽略S发来的任何数据分 组，只等待连接确认应答分组。而S在发出的分组超时后，重复发送同样的分组。这样就形成了死锁。</p>
<p>【问题4】如果已经建立了连接，但是客户端突然出现故障了怎么办？</p>
<p>TCP还设有一个<strong>保活计时器</strong>，显然，客户端如果出现故障，服务器不能一直等下去，白白浪费资源。服务器每收到一次客户端的请求后都会重新复位这个计时器，时间通常是设置为2小时，若两小时还没有收到客户端的任何数据，服务器就会发送一个探测报文段，以后每隔75秒钟发送一次。若一连发送10个探测报文仍然没反应，服务器就认为客户端出了故障，接着就关闭连接。</p>
<p> <img src="/2020/05/04/TCP%E8%BF%9E%E6%8E%A5%E7%9A%84%E5%BB%BA%E7%AB%8B%E5%92%8C%E5%85%B3%E9%97%AD/tcpip%E5%8D%8F%E8%AE%AE%E6%A0%88.jpg" alt="img"> </p>
]]></content>
  </entry>
  <entry>
    <title>计算机网络</title>
    <url>/2020/08/19/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/</url>
    <content><![CDATA[<p>Linux查看域名对应的ip地址</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nslookup www.baidu.com</span><br><span class="line">ping www.baidu.com</span><br><span class="line">host www.baidu.com</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<p>TCP 和 UDP 头部比较</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">TCP头部占 至少 20 字节</span><br><span class="line">	1 源端口和目的端口 共占  4 字节</span><br><span class="line">	2 序列号 syn 占 4 字节</span><br><span class="line">	3 确认号 ack 占 4 字节</span><br><span class="line">	4 长度 + 标志位（SYN,FIN, ACK, ...）+ 窗口大小 占 4 字节</span><br><span class="line">	5 校验和 + 紧急指针 占 4 字节</span><br><span class="line"></span><br><span class="line">UDP头部 占 8 字节</span><br><span class="line">	1 源端口和目的端口 共占  4 字节</span><br><span class="line">	2 整个数据报文长度 + 校验和  占 4 字节</span><br></pre></td></tr></table></figure>

<p>OSI 七层协议：</p>
<p><img src="/2020/08/19/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/%E5%90%84%E5%B1%82%E5%8D%8F%E8%AE%AE.png" alt></p>
<p>各层协议对应端口号</p>
<p><img src="/2020/08/19/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/%E5%8D%8F%E8%AE%AE%E7%AB%AF%E5%8F%A3%E5%8F%B7.png" alt></p>
<p>各层协议使用的机器，机器的作用</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">传输层：网关</span><br><span class="line"></span><br><span class="line">网络层：路由器、防火墙</span><br><span class="line"></span><br><span class="line">数据链路层：网卡、网桥、交换机</span><br><span class="line"></span><br><span class="line">物理层：中继器 （Repeater，也叫放大器） 、集线器</span><br></pre></td></tr></table></figure>

<p>路由表中有哪些信息？</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">路由表中表项内容一般包括:</span><br><span class="line">1.destination:目的地址,用来标识IP包的目的地址或者目的网络。</span><br><span class="line">2.mask:网络掩码,与目的地址一起标识目的主机或者路由器所在的网段的地址。</span><br><span class="line">3.pre:标识路由加入IP路由表的优先级。可能到达一个目的地有多条路由</span><br><span class="line">4.nexthop:下一跳IP地址,说明IP包所经过的下一个路由器。</span><br><span class="line">5.interface:输出接口,说明IP包将从该路由器哪个接口转发。</span><br></pre></td></tr></table></figure>

<p> 一个计算机新进入了一个局域网，他是怎么知道自己ip的？ </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">DHCP(动态主机配置)协议，新机子进来之后首先会广播自己的MAC，然后DHCP Server会租给新机子一个ip地址</span><br><span class="line"></span><br><span class="line">DHCP可以动态的为其管辖下的主机分配地址。DHCP协议报文是UDP报文，DHCP使用67（客户端）和68（服务器）端口。</span><br></pre></td></tr></table></figure>





<p>各层分别有什么作用</p>
<ul>
<li>链路层：负责封装和解封装IP报文，发送和接受ARP/RARP报文等。</li>
<li>网络层：负责路由以及把分组报文发送给目标网络或主机。</li>
<li>传输层：负责对报文进行分组和重组，并以TCP或UDP协议格式封装报文。</li>
<li>应用层：负责向用户提供应用程序，比如HTTP、FTP、Telnet、DNS、SMTP等。</li>
</ul>
<p><img src="/2020/08/19/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/tcpip%E5%8D%8F%E8%AE%AE%E6%A0%88.jpg" alt></p>
]]></content>
  </entry>
</search>
